// Generated from docs/ by tools/docs.mjs — edit the Markdown, not this file.
// `c3c build` regenerates it; `docs.js` reads it.

export const DATA = {
	"version": "0.1.0",
	"summary": "A Three.js-shaped scene API over direct Vulkan.",
	"differences": {
		"load-is-synchronous": "`three.load(path)` is synchronous. `await` works but is not needed.",
		"measure-everything": "Everything placeable can be measured — measure rather than guess. A kit piece's origin is wherever its exporter left it, so a hand-written size table is the thing that goes stale and sinks pieces into walls.\n\n- `asset.mesh(name).bounds`, `geometry.bounds` — a Box3 in the piece's own space, read out of the glTF JSON, so it costs no upload.\n- `object.boundingBox()` — the world-space box of a subtree.\n- `object.boundsInParent()` — the same box in the parent's frame, and it works before `add()`.",
		"align": "`object.align(axis, edge, at)` moves an object until one face of its box sits at a coordinate. `object.alignTo(other, { axis, mine, theirs, offset })` says the same against a sibling.\n\n```js\npiece.align('y', 'min', 0);        // stand it on the ground\npiece.align('z', 'min', wallZ);    // back flush with a wall\n```\n\nBoth work in the parent's frame, because that is the frame a script writes positions in. `alignTo` refuses objects with different parents rather than being wrong by whatever the parents differ by. Set rotation and scale first — they are inputs to where the box is.",
		"debug-draw": "There is debug draw, and reaching for it is the cheap move.\n\n- `three.BoxHelper(object)` — what an object actually occupies.\n- `three.Box3Helper(box)` — a Box3 you worked out yourself.\n- `three.AxesHelper(size)` — where a pivot is and which way it faces.\n- `three.GridHelper(size, divisions)` — where the ground is.\n- `three.WireframeHelper(mesh)` — a mesh's own edges, which is how two faces 0.01 apart are found: a z-fighting starburst is invisible in a solid render.\n\nThey are ordinary meshes. A thousand of them are one draw call, `helper.color` is per copy and free, `scene.remove(helper)` works, and they are not pickable — a click goes through the box onto the thing inside it.",
		"helpers-draw-over": "Helpers draw over everything: the line pipeline tests no depth, unlike Three.js's helpers. That is deliberate — the times you ask where something is are the times it is inside a wall.\n\nThe cost of being ordinary meshes runs the other way: a helper draws, so it is inside `boundingBox()`, inside the `boundsInParent()` of whatever it hangs from, and inside `three.camera.frameAll()`. Align first and add helpers after, or hang them from a Group of their own.",
		"helper-material": "A helper cannot be given a ShaderMaterial. A material is a pipeline and every pipeline draws triangles, while a helper's indices are pairs — assigning one would read the pairs as triangles rather than fail, so it throws instead. `helper.color` is the knob a helper has.",
		"geometry-kinds": "Geometry is BoxGeometry, SphereGeometry, PlaneGeometry, CylinderGeometry, ConeGeometry, TorusGeometry and ConvexGeometry, built for you with Three.js's signatures, defaults and orientations.\n\nThere is no BufferGeometry, no attribute access and no way to read or write a vertex. That refusal is what makes every scene one instanced draw per unique shape.",
		"convex-geometry": "`new three.ConvexGeometry(points)` is the way to make a shape that is not one of the six parametric ones: hand over a cloud of points and get its convex hull — rocks, crystals, gems, debris, the bound of a scan.\n\nIt takes Vector3s, `[x, y, z]`s or a flat array of coordinates, needs at least 4 points and is capped at 65536. It is flat shaded, because a hull's faces meet at hard creases, and textured face by face: each facet gets its own planar projection at one uv unit per unit of local space, so a map is the same size on a hull as on a box beside it. The points are a description the hull is computed from, not the mesh's vertices — most are discarded and none can be read back.",
		"geometry-identity": "Two geometries with the same numbers are one asset and one draw call, however many times you construct them. Two different sizes are two. Prefer `mesh.scale` over a new size when you want variety cheaply.",
		"mesh-construct": "`new three.Mesh(geometry, material)` takes either a generated shape or `asset.mesh(name)`. Material is optional, as in Three.js.",
		"color-and-variant-only": "`mesh.color` and `mesh.variant` are the only two things copies sharing a geometry and a material may differ in without becoming separate draw calls. A thousand meshes in a thousand colours is one call; giving two of them different materials is two. There is no InstancedMesh because every mesh is already an instance.",
		"uniform-tables": "A ShaderMaterial uniform may be a table: `{ palette: [[1,0,0], [0,1,0]] }` becomes `float3 palette[2]` and `mesh.variant` picks the row. That is how one material gives many meshes many looks.\n\n`s.variant` is clamped to the table, so an index past the end is the last row. A table is not capped at the push block — past 104 bytes the array columns move to a device buffer on their own and the body is unchanged, so hundreds of rows is ordinary (up to 256 KiB, which is thousands). Plain uniforms, the ones with no rows, stay in the push block and are still held to that 104.",
		"vertex-stage": "A ShaderMaterial has a vertex stage as well as a fragment one, and it moves geometry per vertex with no draw call, no upload and no geometry change — a thousand copies are still one call.\n\n```js\nnew three.ShaderMaterial({\n  vertex: `void displace(inout Vertex v) { v.position.y += sin(v.local.x * 3 + t) * 0.4; }`,\n  bounds: 0.4,\n})\n```\n\n`Vertex` is the varyings: `position` (world), `normal`, `uv`, `color` and `variant` are read back after your body runs; `local` (object space) and `index` (the vertex number) are inputs. The normal is not recomputed for you. Always pass `bounds` — the number of world units the body can displace by — because culling tests the mesh's undisplaced box and geometry outside it is dropped while still on screen.",
		"one-module": "The vertex body and the fragment body compile into one Slang module, vertex first. A helper function may therefore be declared in only one of them: declaring `float3 ripple(float2 q)` in both is `error[E30201]: function 'ripple' already has a body`, which is correct and surprising. Put shared helpers in `vertex`, which comes first, and call them from `fragment`.",
		"material-samplers": "A ShaderMaterial or a post pass may declare up to four samplers of its own: `{ textures: { noise_map: tex } }` makes `noise_map.Sample(uv)` work in the body.\n\nYou never write a binding number — the shader is generated with the bindings in it and the host resolves each name through the compiled module's reflection, so adding one at the front of the list renumbers nothing. `material.map` is separate and is still the base colour image. A sampler declared and left null reads 1x1 white rather than reading nothing, and both objects are live: `mat.textures.noise_map = other` swaps the image with no compile.",
		"no-colour-management": "Colours are linear rgb in 0..1 — hex is divided by 255, not de-gamma'd. There is no colour management here, and half of one would be worse than none.",
		"many-scenes": "A Scene is an ordinary object and you can hold as many as you like. `new three.Scene()` makes one and shows it; it does not empty or free the one before it.\n\nExactly one scene is rendered at a time — `scene.activate()` is the switch, `scene.isActive` says which — and only that one is drawn, culled, queried and stepped. That is the level transition: build the next scene while the current one is on screen, activate it, dispose the old one, then `three.unloadUnused()`.\n\nThe look travels with the scene: `scene.background`, every light in `three.lights` and `three.light.shadow` belong to the active one, and activating a scene again brings back the lights it had, so a level does not come home to the default sky.\n\nTwo consequences worth knowing:\n\n- Bodies added to an inactive scene are correct and motionless, because the frame steps one world. A nav bake is not — baking is voxelization over that scene's own triangles — so the next level's pathfinding can be solved before anybody sees it.\n- Nothing frees a scene for you. `stats().scenes` is the count, `three.scenes` is the list behind it, and `three.disposeInactive()` frees every world except the rendered one. A game that transitions eight times and never disposes holds eight worlds.",
		"manual-free": "Nothing is freed until you say so.\n\n- `scene.unload()` — empty the scene and give back every asset and texture nothing else holds.\n- `scene.dispose()` — free the whole scene, its bodies and its bake with it.\n- `three.unloadUnused()` — the freeing without the emptying.\n\nNone of them is a garbage collector: resident memory that depended on when the interpreter felt like collecting would be the worst possible property for the one number a game watches. `stats().assets` is how you watch it work.",
		"stale-handles": "An asset handle goes stale when the asset is unloaded, because the host reuses the slot. Naming one throws a sentence saying so, and it throws at the `new three.Mesh()` that named it rather than at the `scene.add()` that would have drawn it — so the line that is wrong is the line that is blamed, which matters in a script that builds a subtree and adds it at the end.\n\nLoading the file again gives a fresh handle. Object handles go stale the same way, and the event is `scene.dispose()` rather than `new three.Scene()` — a scene you stopped rendering still holds its objects, so its handles still resolve.",
		"camera-is-a-turntable": "There is one camera and it is a turntable: `three.camera.orbit(yaw, pitch, distance)` and `three.camera.frameAll()`. It is read-only through accessors.\n\n- `camera.yaw` / `.pitch` / `.distance` / `.fov` / `.near` / `.far` read back.\n- `camera.position()` / `.forward()` / `.right()` give the eye and the look and strafe directions in world space.\n- `camera.planarMove(fwd, strafe)` turns a W/A/S/D input into a world direction.\n\nThere is no `camera.position` to assign, and `yaw`/`pitch`/`distance` throw if assigned.",
		"spatial-queries": "Bulk spatial queries go through an index rather than a scan: `three.query.sphere(point, radius)`, `three.query.box(box)`, `three.query.raycastAll(origin, direction)` and `three.query.sweep(from, to, { radius, height })`. `scene.raycast` goes through the same index.\n\nThe index refreshes on the first query after anything moved and at no other time, so a frame that asks nothing pays nothing. Moving something and then asking about it is fine — a moved node is re-filed rather than the index rebuilt. Toggling `object.visible` costs nothing at all, since an invisible node is skipped when a query answers. What does rebuild the index is structure: adding, removing or re-parenting a node, or setting `object.collides`.\n\nEvery verb comes in two forms. `three.query.sphere(p, r)` allocates an Array of objects, which is right for a click or a one-off; `three.query.sphere(p, r, buffer)` fills a `three.query.buffer(n)` you keep and answers with a count, which is right for the loop.\n\n`box` and `sphere` are broad phase — box against box — so a node whose box overlaps and whose triangles do not is included. `raycastAll` and `sweep` are exact.",
		"not-collision-geometry": "Every drawable mesh is swept and raycast against, so a pickup lying on a path is a bollard and a field of grass is a fence. `object.collides = false` is the fix: it takes that one mesh out of the spatial index entirely — `three.moveAndSlide` walks through it, `three.query.sphere` does not report it, `scene.raycast` misses it — while it still draws exactly as before.\n\n`three.physics` is untouched by it, so a mesh with `collides = false` and a `{ trigger: true }` body is the ordinary way to write a pickup you can walk into and cannot walk into.\n\nIt is not inherited — set it on the meshes, not on a Group, because it says what one piece of geometry is — and it is an authoring flag, not a per-frame one. To hide a character from one sweep, pass `{ ignore }` on that call; to hide something from the frame, `object.visible = false`, which costs the index nothing where toggling `collides` costs a rebuild.\n\n`stats().colliders` is how many meshes are in the index. Reading it beside `stats().nodes` is how you find out that a level is mostly scenery pretending to be walls.",
		"systems-and-casts": "A big animation loop is the problem this solves, not a slow one. `three.systems.step(name, fn)` and `three.systems.frame(name, fn)` are an ordered, named list replacing the one callback `setAnimationLoop` and `setFixedLoop` each take: the verb is the clock, they run in registration order, and `{ before: 'other' }` moves the one that has to break that.\n\n`three.systems.report()` gives per-system milliseconds — the CPU half of what `three.stats()` has done for the GPU half all along. `setAnimationLoop` and `setFixedLoop` are systems under reserved names, so nothing already written changes, and a system that throws does not stop the others.\n\n`three.cooldown(duration, { recover, phase })` rides the same registry: a scalar timer — `active`, `ready`, `recovering`, `remaining`, `progress` — ticked by its own system rather than read off `three.clock`, because the clock advances once per host tick and a coyote-sized window can span several fixed steps of it.\n\nThe storage half is `three.Entity` — see entities-and-rules. None of this makes a frame faster. What it buys is a frame that reads as a list of named things, and a slow one you can attribute.",
		"entities-and-rules": "A game is entities and the rules between them. `class Critter extends three.Entity` is how a plain JavaScript class becomes one — there is no registration call. The class registers itself on first use, reading `static capacity` / `columns` / `parent` / `body` / `volume` / `trigger` / `collides` off itself. Use `Class.spawn(...)` rather than `new Class(...)`, because the slot has to exist before the constructor runs. `three.track(Class, options)` is the same registration for a class that already has a parent.\n\nIt owns the object → instance map, the spawn ritual, the physics body, the collision volume, the live list and the deferred compaction. So `Class.of(hit.object)` is the instance, and `c.remove()` is the whole removal — body, volume, node and map entry, on this tick.\n\n- `static volume = { shape: 'capsule', radius, height, offset }` is an invisible kinematic body beside the drawn node, carried by `<Class>.follow`. `static trigger` is the same with `trigger: true`, which gives a player moved by `three.moveAndSlide` something to trip.\n- `c.hp = 3` is an ordinary field on an ordinary object.\n- Fields a bulk verb reads are declared as `{ columns: { position: 3 }, capacity: N }`, and the instance holds a subarray window onto the shared column — so `three.steer(Critter.column('position'), ...)` is handed the storage itself and nothing is gathered.\n\nThe rules are the other half. `Class.on(event, matcher, fn, options)` dispatches on a pair, subject first: the rule lives on `Critter`, so the handler is handed `(critter, player)` in that order. The matcher is a tracked class, an Object3D, `'*'`, or a class name as a string for a forward reference.\n\nEvents are `enter`/`exit` from a trigger, `touch`/`separate` from a contact, `click` from the raycast, and `near` from `{ within }` — a distance test the engine runs, and the one physics never raises, since `three.moveAndSlide` produces no contact at all. Anything else is the game's own, through `three.emit(a, verb, b)`.\n\nEngine events queue and are drained by one system named `rules`; `three.emit` dispatches at once, because the game knows when it is safe to delete something and the solver does not. Rules replace by name so a hot reload does not double them, and one that throws is contained, named and counted.",
		"crowds-in-one-crossing": "Three verbs scale with a pack, and the rule behind them is that a verb called once per agent per frame answers into memory you already own.\n\n- `three.steer(positions, velocities, options)` — seek, arrive and separation for the whole crowd.\n- `three.moveAndSlideAll(positions, motions, options)` — the character controller for the whole crowd, updating positions in place, with `self` as the column of handles that keeps each agent out of its own mesh and an optional `three.moveBuffer(n)` for the grounded/slope/normal answers.\n- `field.sample(positions, { costs, directions })` — the flow field for the whole crowd.\n\n`three.batch(objects, { euler: true }).flush()` then draws them in one more crossing. At 200 agents the same frame is 2.20 ms with the single-agent verbs and 0.51 ms with these, almost entirely the per-agent JavaScript objects the single forms build.\n\nThe single forms are right for one character — the player, the thing riding the moving platform — because they answer with node handles and a readable object. The bulk ones are right the moment there is a loop around the call. They disagree in one place: a negative cost is unreachable in the bulk sampler, where `field.cost` answers Infinity.",
		"move-and-slide": "`three.moveAndSlide(position, motion, options)` is the character controller: it sweeps a capsule, slides along what it hits, climbs a ledge under the step height and reports whether it is standing on anything.\n\nIt takes a position and answers with a position. It owns no object, integrates nothing and remembers nothing between calls, so gravity, the velocity and the jump stay yours.\n\nOptions are `{ radius, height, step, slope, skin, snap, ignore }`. `height` is the whole capsule. `slope` in degrees decides grounded, whether a ledge is climbed and whether a contact is a floor or a wall — all from one number, so the three cannot disagree.\n\nPass the character's own object as `ignore` or it collides with its own mesh, and pass the Group rather than one mesh out of it: `ignore` leaves the whole subtree out, so a character built from a body, a head and four limbs does not collide with its own chest. It takes an array too, for up to eight things.\n\nIt is kinematic and touches no rigidbody. A character built out of physics is pushed by contacts, tips over and answers a frame late.",
		"navigation": "`three.nav.bake({ cell, radius, height, slope })` voxelizes the scene's standing room, and nothing bakes it for you — call it after the level is built.\n\nThen two verbs, and the split is the design: `three.nav.path(from, to)` is one agent's route, and `three.nav.field(goals)` is a solve kept that a whole crowd samples. A path solves the entire reachable set and throws it away, so a hundred agents heading for one door is a hundred solves for one field. A field has `direction(point)`, `cost(point)` — Infinity for unreachable — and `dispose()`.\n\nPaths come back shortened against the actual geometry with a capsule sweep at the agent's own size, so they do not look like they are walking cell centres, and their waypoints sit on the floor.\n\n`cell` decides everything: it is the resolution and the largest step that can be climbed, because two cells are connected when they are adjacent and one cell up. `three.nav.stats()` reports voxels, walkable and bakeMs, which is how you find out whether the bake is a level-boundary operation or a loading screen.",
		"steering": "`three.steer(positions, velocities, options)` fills a Float32Array with a desired velocity per agent — seek, arrive and separation, for the whole crowd, in one crossing. `positions` is three floats per agent and is read; `velocities` is three floats per agent and is written.\n\nOptions are `{ field, goal, maxSpeed, arrive, separation, separationWeight }`; a field wins over a goal, because a field already knows the way round a wall.\n\nWhat comes back is a desired velocity, not a position. Integrating it and deciding whether an agent may actually go there are yours, which is what lets the same call feed `three.moveAndSlide` for agents that collide and a plain add for agents that do not.",
		"batched-transforms": "`three.batch(objects)` moves many nodes in one crossing, through a Float32Array.\n\nIt is not a faster way to move a dozen things: five hundred ordinary `mesh.position.set` calls measure 0.245 ms a frame, three per cent of the budget, and the trigger for this is about two thousand nodes a frame. It is for the case where the write is already a loop over numbers — a crowd steered by `three.steer`, a particle field, a chunked terrain.\n\n`batch.positions` is a Float32Array seeded from where the objects are now; `batch.flush()` writes them and answers with how many landed. With `{ trs: true }` the stride is ten floats: position, an xyzw quaternion, then scale.",
		"window-is-not-the-picture": "The picture follows the window. Everything renders into an offscreen target and the swapchain puts that on the window; when the window moves — a drag, `three.window.resize`, fullscreen, a display of a different density — the target moves with it, so what is on screen is its own pixels rather than stretched ones.\n\n`--width`/`--height` are the size the window opens at, and stay the target's own size under `--headless`. So `three.renderSize()`, the PNG a screenshot comes back as, and the coordinates `scene.pick(x, y)` counts in all track the window — on a retina display that is twice the logical size and four times the pixels to shade.\n\n`three.setRenderSize(width, height)` pins it: a target below the window is upscaled to fill it, which is the render-scale slider a settings screen has. While pinned the window no longer moves the picture. `three.setRenderSize(null)` gives the follow back.\n\n`three.window.width` and `.height` are device pixels read off the drawable rather than off the window, so they are current through a live resize drag; `.scale` is device pixels per logical point.\n\nResizing is a request, not a command: X11's window manager may adjust it and Wayland answers with a configure some frames later, so the new size arrives on a later frame. Under `--headless` the sizes read zero and `resize()` returns false rather than throwing. On Wayland the size never reads back at all — that surface answers \"whatever the swapchain asks for\" — so it is the one platform where the picture cannot follow.",
		"the-interface": "There is a UI, and it is drawn into the frame rather than over the window: `three.ui.set(tree)` describes a panel, `three.ui.patch(key, props)` changes one value in it, and `three.ui.draw(ops)` is the screen-space layer a crosshair and a health bar live in. It goes into the same offscreen target the scene does, after the post chain, so `--screenshot` and the MCP screenshot carry it.\n\n- It is retained. A node that did not change costs nothing to redraw: `set` when the shape changed, `patch` when a number did, never rebuild every frame.\n- It arbitrates the pointer. A click on a button does not also orbit the camera or reach `three.onClick`, and typing in a text field is not also WASD. A caption, a panel background and a drawing stay transparent to the pointer unless they say `solid: true`.\n- Give anything you type into, scroll or open a `key`. `set` rebuilds the tree, and the key is what carries the text, the scroll position, the open popup and the keyboard focus across the rebuild.\n\n`three.Widget` does the set/patch bookkeeping for you: `class Hud extends three.Widget` with one `render()` describing the interface as it is now, composed out of the node classes on `three.ui` (Panel, Label, Button, ...). Assigning a field re-renders and sends only what changed.\n\nColours are linear like everything else here, so `0x808080` is the same grey a material would be.",
		"pointer-lock": "`three.input.pointerLock = true` takes the mouse pointer out of the user's hands, and what it buys is a look that does not stop. Without it `three.input.pointer.dx` is a difference of cursor positions, and a cursor stops at the edge of the screen while a hand does not.\n\nReading the property back tells you whether the platform gave it, not what you asked for: a headless run has no window and always reads false, and so does a backend with no implementation. Nothing throws, so a game can fall back to a drag-look rather than refuse to start. `three.input.pointer.locked` is the same fact reported beside the deltas it is about.",
		"scalar-math": "The MathUtils block is on `three` itself: `clamp`, `clamp01` (GLSL's saturate), `lerp`, `inverseLerp`, `mapLinear`, `smoothstep`, `smootherstep`, `band`, `pingpong`, `euclideanModulo`, `degToRad`, `radToDeg`, `moveTowards`, plus `wrapAngle` / `angleDelta` / `moveTowardsAngle` for the seam at ±pi and `mixColor` / `tintColor` for colours.\n\nThree.js's names and Three.js's argument order, which matters for exactly one of them: `smoothstep` here is `(x, min, max)` and GLSL's is `(edge0, edge1, x)` — the shader body and the script above it take the same three numbers in different orders, and swapping them is silent.\n\nRandomness is `three.randFloat` / `randInt` / `randFloatSpread`, drawn from a seeded stream `three.seed(n)` resets rather than from `Math.random` — one `Math.random()` in the gameplay layer throws away the determinism the fixed step exists for. Noise is `three.hash` / `noise2` / `fbm2`, sampled at a point, with a `period` option that makes a texture tile.\n\nNone of this crosses to the host: a host call that allocates to answer arithmetic measures 185 ns against the 70 ns of the JavaScript it replaced.",
		"damping-and-curves": "`three.damp(current, target, lambda, dt)` and `three.smoothDamp(current, target, state, smoothTime, dt)` are the two verbs between \"where it is\" and \"where it should be\", and both are frame-rate independent. That is why they are named rather than written inline: `x += (target - x) * 0.1` closes a tenth of the gap per frame, so it is twice as fast at 120 Hz as at 60.\n\n- `damp` is a decay — a camera easing onto a target.\n- `smoothDamp` is a critically damped spring with momentum — a turret slew, a sliding panel. Its state object must outlive the frame or it is re-launched from rest every tick.\n- `three.dampAngle` takes the short way round a circle, which is what a heading needs.\n\n`dt` is in seconds, and so is `three.clock.dt` — pass it straight through, or the `dt` a `three.systems` system is handed, which is the same number.\n\n`new three.CatmullRomCurve3(points)` is the curve a loop samples: `getPointAt(u)` walks its length and `getPoint(t)` walks its own parameter, and the difference is what makes hand-written rail code look wrong.",
		"camera-follow": "The camera can follow something: `three.camera.attach(object, { offset, distance, lag })` puts the orbit point on that object every frame — after the animation, the physics and your animation callback have all moved it, so the camera is never a frame behind, which is what makes a trailing camera look like the character sliding.\n\n`three.camera.detach()` stops; `three.camera.attached` is what it is following, or null.\n\nA drag still orbits and the wheel still zooms while attached. A pan is the one gesture that stops working, because a pan writes the orbit point and the next frame writes it back.\n\nThere is one camera and it follows something in the scene being rendered: attaching it to an object in a scene that is not throws, and activating a scene detaches it.",
		"camera-first-person": "First person is distance 0, and it is not a mode: the eye sits on the point it orbits, so `three.camera.attach(character, { offset: [0, 1.7, 0], distance: 0 })` is a person and scrolling back out is a third-person camera again with nothing to switch.\n\nThe offset is where the head is, in world space — `[0, 1.7, 0]` is the same vector whichever way a character faces. Aim it with `three.camera.orbit(yaw, pitch)`, leaving the distance argument off, and `three.input.pointer.dx`/`.dy` is what a mouse look feeds it.",
		"camera-planes-derived": "The near and far planes are derived, not set: from the orbit distance and from the scene's own bounds, every time the camera moves. Three.js makes them constructor arguments to PerspectiveCamera.\n\nRead them — `three.camera.near` and `.far` — when something has stopped being drawn, because geometry past far is absent rather than dim and is culled as well as clipped. Assigning either throws rather than being ignored.",
		"lights": "Up to four directional lights, and none of them is an Object3D. `three.lights` is the list; `three.lights[0] === three.light` is the sun, the only one that casts a shadow, which is why the shadow settings hang off it.\n\nEach has `direction` (a world-space surface-to-light vector, live: `three.light.direction.y = -1` writes through), `color` (white by default) and `intensity` (1 by default, multiplying the colour, so it is how a light goes brighter than white).\n\n`three.lights.add([1, 0, 0], 0x4060ff, 0.5)` fills the next slot and answers with it; `three.lights.remove(i)` closes the gap. Light 0 cannot be removed — set its intensity to 0. Adding a fifth throws.\n\nNot `scene.add(new DirectionalLight(...))`: a light here has no position, nothing can be parented to it and `scene.remove` does not reach it. A direction is not normalized, so it reads back as you wrote it, and a zero one throws rather than turning every shaded pixel into a NaN.",
		"ambient": "`three.light.ambient` is the floor a face turned right away from every light gets, 0 to 1: at 0 it is black, at 1 there is no shading at all and everything is its own flat colour.\n\nIt is on `three.light` rather than on each light because it is not a light — it stands in for every bounce this renderer does not simulate, and four of them summed would be four times the same fudge. `three.light.set(direction, ambient)` sets the sun and the floor at once; the second argument is the floor and not a colour. Defaults to 0.25, and a new Scene restores it.",
		"specular": "There is a specular term and it is off on every material until you ask.\n\n- `material.reflectance` is the switch: 0 by default (no highlight at all), and 0.5 is the 4% that ordinary dielectrics reflect — what to use for anything wet, polished or glazed.\n- `material.roughness` spreads the highlight out. 1 by default and perfectly diffuse.\n- `material.metalness` moves a surface's colour out of the diffuse and into the highlight. With no environment map to reflect, a fully metallic surface is its highlights and the ambient floor and nothing else, which is correct and is dark.\n\nIn a ShaderMaterial body, `standard(s)` is the whole built-in shading, `specular(s)` is the highlight alone and `lambert(s.normal)` is the diffuse alone — a body written against `lambert` is matte and stays matte.",
		"shadows-off-by-default": "The sun casts a shadow, and it is off until you ask: `three.light.shadow = true`, or `three.light.shadow = { enabled: true, size: 4096 }`.\n\nThe four properties are `enabled`, `size` (texels per side, clamped to 256..8192 and rounded down to a power of two), `bias` (extra depth offset in the light's clip space, 0 by default) and `intensity` (how dark, 0 to 1). Nothing is allocated and no shader is compiled until the first frame with it on, and a new Scene turns it back off.",
		"shadow-cast-receive": "Everything opaque casts and everything shaded receives. There is no `castShadow` or `receiveShadow` per object, because two copies of one mesh disagreeing about it would be two draw calls.\n\nA transparent material casts nothing — a shadow map holds one depth per texel, so glass would have to be either solid or absent, and absent is the better wrong answer — and neither does a debug helper.\n\nA ShaderMaterial receives shadows with no change to its body: `lambert()` already has the shadow folded into the direct term, and `s.shadow` is the raw factor for a body that wants it separately.",
		"shadow-one-map": "One map, fitted around the whole scene every frame, so its resolution is `size` divided by however wide the scene is. If shadows look blocky the scene is large, not the map small: raise `three.light.shadow.size`, or draw the part that matters and leave the rest out. There are no cascades.\n\nSelf-shadowing stripes should not appear — each sample is lifted two texels along its own normal first — and if they do, `three.light.shadow.bias` is the knob, in small numbers like 0.0005.",
		"shadow-costs-a-draw": "A shadow pass costs a second draw call per opaque bucket — `stats().shadowDraws` is the count — and it turns frustum culling off for the frame, because a caster the camera cannot see still throws a shadow into the frame. So `stats().culledLastFrame` reads 0 while shadows are on; neither costs draw calls, since culling here drops instances from buckets, never buckets.\n\nMark the scenery `object.static = true` and that second pass all but disappears — see static-casters.",
		"background-is-a-colour": "`scene.background` is a colour or null, never a Texture: `[r,g,b]`, `0x87ceeb`, or null for the default. There is no environment map and no `scene.environment`. A gradient sky is still geometry — what this removes is having to build one to escape the default near-black.",
		"colours-are-srgb": "Every colour you state is sRGB — the components a colour picker gives. `mesh.color = 0xff8040` renders as `0xff8040` under a full light, `scene.background = 0x2060a0` screenshots as `0x2060a0`, and a texture's bytes come back out as the bytes that went in.\n\nThe shading arithmetic in between is linear and the conversion is the renderer's job, so nothing in a script should apply a gamma of its own; a scene that pre-corrects its own textures will now be twice corrected.",
		"side-is-on-the-material": "`material.side` is on the material and not on the mesh, because it is a property of the pipeline: two meshes sharing a geometry and a material are one draw call and would stop being one if they could disagree about it.\n\n`three.BackSide` is how a skydome is made visible from inside. Scaling a sphere by -1 does not work, because a negative scale does not reverse a triangle's winding.",
		"add-to-add": "An object is not in the scene until it is `add()`ed, and removing it makes it a detached description that can be added again.",
		"group-for-belonging": "A Group is how several objects stay one object. Nothing else records that they belong together: siblings built by one loop and placed by the same arithmetic have no relationship the scene graph can see, so a later edit that moves one leaves the others where they were.\n\nParent the pieces of a thing to a Group, place them relative to it once, and move the Group instead. It costs a node and no draw call.",
		"name-things": "`name` is empty until a script sets it and `getObjectByName` answers null for a miss, both as in Three.js — so a node nobody named is reachable only through `traverse`, and a misspelled one is a null that throws somewhere else. Name whatever a later script will look for.\n\n`asset.instantiate()` trees need no help: the root takes the file name and every node under it keeps the name the file gave it.",
		"fragment-not-program": "ShaderMaterial takes a fragment function, not a whole program: you write `float3 shade(Surface s)` and three.c3 supplies the vertex stage, the Surface and the uniform block. Uniforms are flat values, not Three.js's `{ value }` wrappers.",
		"post-is-a-chain": "There is post-processing and it is a chain, not an EffectComposer: `three.setPost({ fragment })` runs a `float3 post(Post p)` over the finished frame, `three.addPass({ fragment })` puts another after it, and `three.setPost(null)` stops all of them.\n\nThere are no render targets to manage and no dependency declarations — a pass reads what the pass before it wrote as `p.color` and the frame as the geometry left it as `p.scene`, and those two are the whole model.\n\nThe chain runs in linear float, so a pass may return values above 1 and the next one still sees them, which is what a bright pass followed by a blur needs; the encode to the display happens once, at the end. It applies to the window, to `render()` and to screenshots alike, and it belongs to the renderer rather than to the scene, so it survives `new three.Scene()` and outlives the script that set it.",
		"mesh-no-material": "A mesh with no material draws with the base colour and texture its glTF material carried.",
		"no-raycaster": "There is no Raycaster. `scene.pick(x, y)` takes pixels of the rendered image and `scene.raycast(origin, direction)` takes a world ray; both answer with the closest hit or null, not with an array.",
		"script-scope": "Each `run_script` call runs in its own function scope. Use `globalThis` to keep state between calls.",
		"warnings-beside-log": "A `run_script` reply carries a `warnings` array beside `log` when the renderer noticed something, and the two are different things: `log` is what your script printed, `warnings` is what the host has to say about it.\n\nIt is absent on nearly every run, which is what makes it worth reading on the runs it appears in — live scenes climbing, live materials climbing, a shadow map that just cost 134 MB, a shader that would not compile, a disk cache that could not be written.\n\nAdvice, not errors: the run still succeeded. They are deduplicated and they arrive one run late when they happened between calls, so a warning about the frame you just rendered shows up on your next call.",
		"animation-loop": "`three.setAnimationLoop(fn)` runs `fn` once per frame, with the elapsed milliseconds, until `three.setAnimationLoop(null)`. It is how a scene moves without an agent in the loop.\n\nThe callback must be synchronous, is stopped for good if it throws or runs longer than 100 ms in one frame, and what it logs comes back with the next `run_script` under an `[animation loop]` marker.",
		"game-clock": "There is a game clock, which Three.js has nothing quite like: `three.clock.dt` is what the frame being drawn is worth in seconds, `three.clock.time` is what the frames have added up to, and `three.clock.timeScale` is the multiplier — 0 is paused.\n\nIt is not a convenience over differencing the callback's argument yourself. Everything in a frame that moves is downstream of it — the clips, the physics, the fixed loop, the follow camera, the argument `setAnimationLoop` is handed and `p.time` in a post body — so `timeScale = 0` stops the world, which no amount of a script stopping its own arithmetic can do.",
		"fixed-loop-for-gameplay": "Gameplay belongs in `three.setFixedLoop(fn)`, which runs at `three.clock.fixedRate` (60 Hz) however fast frames arrive and hands the callback the same `dt` every call. Drawing the consequence belongs in `setAnimationLoop`.\n\nThe accumulator is the host's: one written in the animation callback spends the script budget catching up and gets the callback stopped for good instead of merely stuttering.",
		"animation-loop-freezes-render": "A running animation loop makes `render()` and `screenshot()` no longer repeatable — the scene has moved between them.\n\n`three.clock.timeScale = 0` is the finer instrument: it freezes the world without unregistering anything, and `three.clock.advance(seconds)` then steps it by exactly as much as you ask, so two runs asking for the same amount draw the same frame. `setAnimationLoop(null)` still stops the callback outright.",
		"keyboard": "There is a keyboard, which Three.js has no equivalent of at all.\n\n- `three.input.isDown(key)` for held keys.\n- `three.input.pressed(key)` / `released(key)` for this frame's edges.\n- `three.onKeyDown(key, fn)` / `three.onKeyUp(key, fn)` to bind an action.\n\nKey names are the browser's `KeyboardEvent.key` lowercased — `three.input.keys()` lists every one. It only reports anything while a window is open: `--headless` has no keyboard.",
		"script-can-press-keys": "A script can press keys itself: `three.input.press(key)`, `three.input.release(key)` and `three.input.releaseAll()`. A pressed key stays down until released, exactly as a finger does, and goes through the same path a real one does — so `isDown`, `pressed`, `released` and every `onKeyDown` handler cannot tell the two apart. It adds to the real keyboard rather than replacing it.\n\nThis is what makes an input-driven scene testable at all: a headless boot has no keyboard, so without it the only way to exercise a character was for the scene to hand its internals to a global.",
		"keys-read-per-frame": "Keys are read once per frame, so `three.input.pressed()` and `three.input.text` mean something inside the animation callback and almost never outside one. `isDown()` is fine anywhere.",
		"mouse-is-one-thing": "There is a mouse, and it is one thing: `three.onClick(fn)` calls `fn(hit, x, y)` with what is under the cursor already picked. `three.input.pointer` is everything else about it for this frame — position, movement, all three buttons and the wheel.\n\nThere is no mouseDown and no drag events: the left button orbits the camera, a press that travels or is held is a drag rather than a click, and the buttons are latches a script polls rather than edges anything dispatches.",
		"mouse-look-and-no-pointer-lock": "A mouse look is `three.input.pointer.dx` and `.dy`, not a difference you take yourself between frames — the host differences the reading the frame is actually drawing with, while two calls from a script straddle it.\n\nWithout pointer lock the movement stops at the edge of the screen, where the platform stops the cursor. A look that must keep turning forever wants `three.input.pointerLock = true` — see pointer-lock.",
		"controls-can-be-taken": "The camera's hand on the window can be taken away: `three.controls.enabled = false` stops the mouse orbiting, panning and zooming, and is what a scene that drives its own camera every frame needs — otherwise the turntable writes yaw and pitch again underneath it and the two fight over one matrix sixty times a second.\n\nIt does not stop `three.camera.orbit()`, which is a script moving the camera on purpose. Turn it back on when the mode ends: there is no gesture that undoes it.",
		"pointer-is-in-image-pixels": "`three.input.pointer` and the click are in the rendered image's pixels, not the window's. `scene.pick(x, y)` and the PNG use the same pixels the click does, whatever size the window is.",
		"script-can-move-the-pointer": "A script can move the pointer and press its buttons: `three.input.movePointer(x, y)`, `three.input.pressButton(button)`, `three.input.releaseButton(button)`, `three.input.scroll(dy, dx)` and `three.input.releaseAll()`. The coordinates are the rendered image's pixels — the same ones `three.input.pointer` answers in and `scene.pick(x, y)` takes — and `button` is 0 left, 1 right, 2 middle.\n\nA held button, not an event, exactly as `press(key)` is a held key. So a drag is four statements that each say what happened rather than one verb that invents the frames in between:\n\n```js\nthree.input.movePointer(120, 80);\nthree.input.pressButton(0);\nthree.input.movePointer(180, 80);   // a frame apart, each of them\nthree.input.releaseButton(0);\n```\n\nIt goes in above the hit test, which is the whole claim: `three.input.pointer`, cui's hit test, `three.onClick` and a `draw` node's `onPointer` are all told the same thing, so a press under an open menu or a modal correctly reaches nothing and a press on a panel does not also click the scene behind it. It adds to the real mouse — the buttons are or-ed with the window's and the wheel added to it, and a placed position stands in for the window's only until the real pointer moves, because two positions are not a third.\n\nThis is what makes an interface testable at all. A headless boot has no pointer, so a panel's buttons, its menus and its drags were reachable only by a person with a window open — which is to say not reachable from a test.",
		"physics-world": "There is a physics world per scene, which Three.js has no equivalent of at all: `object.body = { shape, mass }` describes a body and `three.physics.add(object)` gives the object one.\n\n`scene.physics` is the world that belongs to a scene and `three.physics` is whichever scene is being rendered — so the next level's bodies can be built while the current one is on screen, and they stand still until you activate it, because the frame steps one world.\n\nIt is XPBD with real contacts, friction, restitution, joints and triggers, not a demo. Y is down: `three.physics.gravity` is `[0, -9.8, 0]` and there is no axis to configure.",
		"physics-steer-a-body": "A dynamic body is steered with `three.physics.setVelocity(object, [x, y, z])` and pushed with `three.physics.applyImpulse(object, [x, y, z])` — set a speed for a character, add an impulse for a jump or a hit.\n\nBetween them, a dynamic capsule with a velocity set each frame is a character controller: it walks and it collides, which no combination of the other verbs can do. Reading back is `three.physics.velocity(object)`.\n\nStatic and kinematic bodies refuse both by name, because for those the transform is the only thing that moves them.",
		"physics-owns-transform": "The solver owns a dynamic body's transform, and writing to it throws. That is the one place in this API where two writers are not resolved by last-writer-wins — a solver and a script writing the same transform every frame produce jitter rather than a compromise.\n\nGive the body kind `'kinematic'` to drive it from a script, or `three.physics.remove(object)` to take the body away. A body with mass 0 is static and is not owned, because it never moves.",
		"physics-fixed-60hz": "Physics runs at a fixed 60 Hz whatever rate frames arrive at, and the accumulator is the host's rather than the animation callback's — so a slow frame stutters instead of spending the script budget and stopping the callback for good. A frame that ran very long catches up at most five steps and drops the rest, which is the difference between a stutter and a spiral.\n\nWhat it steps by is game time, so `three.clock.timeScale` scales the world and 0 stops it falling; `three.clock.fixedRate` is the gameplay rate and does not touch the solver's.",
		"collider-from-mesh": "A collider comes from the mesh, not from numbers you supply.\n\n- `'box'` and `'sphere'` are its own bounds.\n- `'capsule'` is the bounds about Y.\n- `'heightfield'` is a TerrainGeometry's own grid of heights — one shape for a whole landscape, at any slope, where the alternative is a chain of invisible boxes and a path forced flat to have them.\n- `'hull'` is the convex hull of its points, from the same quickhull that built a ConvexGeometry, so a convex rock's collider is exactly its own geometry rather than an approximation of it.",
		"export-round-trips": "The scene comes back out with `scene.export(path, options)` — a `.glb` with one mesh per unique geometry, so what the file says about sharing is what the frame says. It round-trips: export it, `three.load` it, and the draw-call count is the same, per-copy colours included.\n\nSibling copies of one shape become a single node carrying an array of transforms (EXT_mesh_gpu_instancing, which any glTF reader can place) with a `_COLOR_0` array holding each copy's `mesh.color`. A reader that does not know `_COLOR_0` gets them in the material's own colour rather than in the wrong place. A copy with no sibling drawing the same shape keeps its name and its own material, and groups are never collapsed. Siblings sharing a shape but not a material do not batch — a colour travels per copy, a texture or a blend mode has no per-copy channel.\n\nLeft out on purpose: helpers and hidden subtrees, because the export is what the frame shows, and ShaderMaterials, because a material here is a Slang pipeline and glTF describes surfaces rather than programs. Pass `{ bake: true }` to run each shader body over its mesh's uv layout and write the answer as a `baseColorTexture` or `baseColorFactor` — the difference between a file that is your scene and a file that is your scene in one grey.",
		"static-casters": "A shadow pass rasterises every caster every frame, which for a village is the largest thing in the frame. Say `object.static = true` on whatever will not move again — buildings, ground, walls, scenery — and those are drawn into the shadow map once and kept; each frame after that draws only what moved.\n\nIt costs no draw call in the colour pass, so marking ten thousand crates is free, and moving one afterwards is safe (the map is rebuilt) rather than wrong. `scene.traverse(o => o.static = true)` is the usual way to say it.\n\nWatch `three.stats().shadowStaticDraws`: 0 on most frames means the cache is holding. Refused on skinned meshes — a pose changes the silhouette without the transform moving — and reading `.static` back is how you see that."
	},
	"classes": {
		"Scene": {
			"construct": "new three.Scene()",
			"note": "An independent world, made and immediately shown. It does not empty or free the scene before it: that one keeps its objects, its bodies and its nav bake until you call `dispose()` on it. That is what lets you build the next level while the current one is still on screen — see the many-scenes topic. It is an Object3D, so moving it moves everything.",
			"properties": [
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"children",
				"parent",
				"animations",
				"isActive (whether this is the one being rendered; only one is, and only that one is stepped, drawn and queried)",
				"id (the host's id for this scene, what `three.sceneById(id)` takes. Read-only)",
				"physics (this scene's own physics world; `three.physics` is whichever scene is active)",
				"nav (this scene's own navigation bake; `three.nav` is whichever scene is active)",
				"static (marks the whole scene as not moving again — see the static-casters topic)",
				"background (the clear colour: `[r,g,b]`, `0x87ceeb`, null for the default, or an equirectangular Texture, which draws as a sky. This scene's own, so setting it on a scene that is not being rendered paints nothing until you activate it)",
				"environment (an equirectangular Texture surfaces reflect, or null. This is what makes `material.metalness` mean something: a metal reflects and does not scatter, so with nothing around it, it renders dark. Separate from `background`, as it is in Three.js. `material.roughness` picks a mip of this image, so a rough metal reflects a blurred sky)",
				"backgroundIntensity (a multiplier on the backdrop, default 1)",
				"environmentIntensity (a multiplier on what the scene reflects, default 1; 0 turns the reflection off without giving the image back)",
				"environmentRotation (radians about world Y. One number that turns both the backdrop and the reflections; Three.js has two, and a metal reflecting a sky pointing elsewhere than the one on screen is a bug rather than a feature)",
				"collides (on the root, which draws nothing; set it on the meshes)"
			],
			"methods": [
				"add(...objects)",
				"remove(...objects)",
				"traverse(fn)",
				"getObjectByName(name)",
				"stats()",
				"activate()",
				"dispose()",
				"unload()",
				"export(path)",
				"pick(x, y)",
				"raycast(origin, direction)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, { loop, speed, time, fade })",
				"stop()",
				"socket(bone)",
				"toJSON()"
			],
			"details": {
				"isActive": "Whether this is the Scene being rendered. Exactly one is.\n\nA scene that is not active is drawn by nothing, stepped by nothing and queried by nothing — which is what makes building the next level while the current one runs cost the frame nothing, and also why bodies you add to it stand still until you activate it.",
				"activate": "Show this Scene, and stop showing whichever one was.\n\nNothing is freed: the scene being left keeps its objects, its bodies and its bake, so activating it again brings the world back exactly as it was — its background, its light and its shadow settings included.\n\nThe one thing that does not come back is the camera's attachment: the follow named an object in the scene being left, so it is dropped and `three.camera.follow` is yours to set again.",
				"dispose": "Free this Scene and everything in it: its nodes, its physics bodies, its nav bake, and the asset references its meshes held.\n\nWhat is actually reclaimed depends on what else holds those assets — two scenes over one kit means disposing either frees nothing, which is right. Follow it with `three.unloadUnused()`.\n\nThe Scene being rendered cannot be disposed; activate another one first, because a frame with no scene to draw is a black window that reads as a renderer fault."
			}
		},
		"Mesh": {
			"construct": "new three.Mesh(geometry, material)",
			"note": "`geometry` is a generated shape (`new three.BoxGeometry(1, 1, 1)`) or a reference from `asset.mesh(name)` / `asset.meshAt(i)`. `material` is optional. N meshes sharing one geometry and one material is one draw call.",
			"properties": [
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"geometry",
				"material",
				"children",
				"parent",
				"color (per copy, free: `[r,g,b]`, `[r,g,b,a]` or `0xff8800`)",
				"variant (per copy, free: which row of the material's table)",
				"static (this will not move again: drawn into the shadow map once and kept)",
				"collides (false takes it out of every spatial query — scenery, not a wall)",
				"animations (empty unless this came from `asset.instantiate()`)",
				"morphs (how many morph targets the geometry has, 0 for most)",
				"weights (per copy, free: how much of each morph target this one wears)"
			],
			"methods": [
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		},
		"MorphTargets": {
			"construct": "mesh.weights — on a geometry that has any",
			"note": "Blend shapes. A glTF morph target is a displacement of every vertex, and `mesh.weights` is how much of each one this copy wears; `mesh.morphs` is how many the geometry has, 0 for anything that is not a blend-shape mesh.\n\nWrite `mesh.weights[1] = 0.5` or `mesh.weights = [0.5, 0]` — a shorter array leaves the rest where they are, so setting one expression on a twenty-target face is one call. Weights outside 0..1 are allowed and exaggerate; glTF does not clamp them either.\n\nAddressed by index, not by name: glTF puts target names in `mesh.extras.targetNames`, which is a convention rather than a required field.\n\nPer copy and free, like `color` and `variant`: the displacements live on the asset and only a handful of floats per copy do not, so two faces built from one head mesh wear different expressions and stay one draw call. An all-zero vector takes the copy off the morph path entirely.\n\nA file that animates its weights just works: `play()` drives them like any other channel, and a crossfade blends them. Morph runs before skinning, so a rigged face morphs and then poses, which is the order glTF specifies.",
			"properties": [
				"length (the target count)"
			],
			"methods": [
				"set(values)",
				"fill(v)",
				"toArray()"
			]
		},
		"Material": {
			"construct": "not constructed — it is what MeshLambertMaterial and ShaderMaterial share",
			"note": "The base both materials extend, exported for `instanceof` and for the properties they have in common. `mesh.material` accepts anything that is one.\n\nAssigning a material to a helper throws whatever kind it is: a helper draws line pairs and every pipeline you can build draws triangles.",
			"properties": [
				"map (a `three.texture`, or null; wins over whatever image the mesh itself carries)",
				"side (`three.FrontSide`, `three.BackSide` or `three.DoubleSide`)",
				"transparent (whether it blends; derived from `blending`, and read-only)",
				"blending (`three.NoBlending`, `three.NormalBlending` or `three.AdditiveBlending`)",
				"opacity (0 to 1, settable and free)",
				"roughness (0 to 1, 1 by default: how spread out the highlight is)",
				"metalness (0 to 1, 0 by default: a metal has no diffuse)",
				"reflectance (0 to 1, 0 by default: how strongly a non-metal reflects, and the switch that turns the specular term on at all)",
				"repeat (`[u, v]`, or one number for both: how many times the map is laid across the surface)",
				"offset (`[u, v]`: where the map starts, in whole repeats)",
				"alive (false once `dispose()` has been called on it)"
			],
			"methods": [
				"dispose()",
				"toJSON()"
			],
			"details": {
				"blending": "Decided at construction and not settable. This device bakes blending into the pipeline, so a change is a new material — which is one line.\n\n`NormalBlending` is what `{ transparent: true }` means and is what glass, water and a foliage card want. `AdditiveBlending` never darkens what is behind it and is what fire, a glow and a beam want.",
				"opacity": "It rides the push block, so writing it costs nothing.\n\nIt does nothing unless the material was built transparent, because an opaque pipeline discards the alpha. That is the hardware's answer, and Three.js behaves the same way.",
				"roughness": "1 is perfectly diffuse, and is what every material here was before there was a specular term.",
				"metalness": "A metal's colour moves out of the diffuse and into the highlight. With no environment map to reflect, a fully metallic surface is its highlights and the ambient floor and nothing else — which is correct, and is dark.",
				"reflectance": "0.5 is the 4% that ordinary dielectrics reflect — use it for anything wet, polished or glazed.\n\nA name Three.js does not have, because Three.js defaults every material to having a highlight and this one defaults to none.",
				"repeat": "1 by default; zero throws, because it maps the whole surface onto one texel.\n\nWithout it a surface maps its texture exactly once, so texel density is a function of how big the mesh is and a 128px image across 100 units is a smear.\n\nIt is on the material rather than on the texture as in Three.js: textures here are deduplicated by content across every file, so a transform on the texture would change every unrelated surface using the same picture. Two densities of one image is two materials."
			}
		},
		"DataTexture": {
			"construct": "new three.DataTexture(data, width, height, { colorSpace, generateMipmaps })",
			"note": "Pixels a script built, uploaded as a texture.\n\n`data` is a Uint8Array (or a plain Array, which is copied) of `width*height*4` bytes in r, g, b, a order, row-major from the bottom-left corner — uv (0,0) is bottom-left, as in Three.js. RGBA8 only, and it is on the device when the constructor returns: there is no `needsUpdate` and nothing to schedule.\n\nDeduplicated against every other texture by content, so generated pixels and the identical `.png` are one upload. It is a Texture in every other way. Generating 256x256 in JavaScript costs about 16 ms before any of this, so build at load rather than per frame. `path` is null; the side limit is 8192.\n\nThe fourth argument is the same options object `three.texture` takes, and both options matter more here: generated pixels are as often a table indexed exactly — a palette, a ramp, a lookup a shader reads — as a picture, and a table wants `{ colorSpace: three.LinearSRGBColorSpace, generateMipmaps: false }`, because its channels are numbers and a blurred mip level of a lookup approximates nothing.",
			"properties": [
				"width",
				"height",
				"path (null)",
				"alive",
				"colorSpace (`'srgb'` or `'srgb-linear'`; fixed at upload)",
				"levels (how many mip levels it got)",
				"generateMipmaps (whether it got a chain — the answer, not the request)"
			],
			"methods": [
				"read(into)",
				"dispose()",
				"toJSON()",
				"toString()"
			]
		},
		"Texture": {
			"construct": "three.texture(path, { colorSpace, generateMipmaps })",
			"note": "A PNG, JPEG or KTX2 on the device. Synchronous — it is uploaded by the time the call returns, so `width` and `height` are readable immediately and there is no `onLoad`. The format is read from the file's first bytes, not its extension.\n\nImages are deduplicated by content and by colourspace: two paths holding the same picture, or a `.png` and the identical image inside a `.glb`, are one upload, though each call still answers with its own handle. Under `--assets` the path is inside the game directory and cannot climb out.\n\nPut it on something with `new three.MeshLambertMaterial({ map })`. 16-bit PNGs are refused by name; save as 8-bit. `read()` copies the pixels back off the device.\n\nThe option worth knowing about is `colorSpace`. It defaults to sRGB, which is right for a picture of something and wrong for a map whose channels are numbers — a normal map, a roughness or metalness or occlusion map, a height field. Those want `three.LinearSRGBColorSpace`, and getting it wrong has no error and no obvious symptom: a normal map read as sRGB has its \"no tilt\" 0.5 decoded to 0.21, so every surface leans the same way and the detail goes soft.\n\nA full mip chain is built unless you say otherwise, so a textured floor stops shimmering at grazing angles. An option this does not have is refused rather than ignored — there is no `magFilter` or `wrapS` here.",
			"properties": [
				"width",
				"height",
				"path",
				"alive",
				"colorSpace (`'srgb'` or `'srgb-linear'`; fixed at upload, so load again to change it)",
				"levels (how many mip levels it got)",
				"generateMipmaps (whether it got a chain — the answer, not the request)"
			],
			"methods": [
				"read(into)",
				"dispose()",
				"toJSON()",
				"toString()"
			]
		},
		"MeshLambertMaterial": {
			"construct": "new three.MeshLambertMaterial({ map, side, transparent, blending, opacity, roughness, metalness, reflectance })",
			"note": "The built-in shader with an image on it — the material to reach for when what you want is a picture on a shape. It compiles nothing and cannot fail with a shader diagnostic.\n\nLambert is what it actually computes by default: the lights, an ambient floor, and no highlight until `reflectance` or `metalness` asks for one. There is no environment.\n\nIt has no `color`, because `mesh.color` is the per-copy channel and multiplies into the sampled texel — so one material tints a thousand copies differently and is still one draw call. With no map it is the cheapest way to ask for a side, which is what a skydome needs.",
			"properties": [
				"map (a `three.texture`, or null; settable)",
				"side (`three.FrontSide`, `three.BackSide` or `three.DoubleSide`; settable)",
				"transparent (derived from `blending`, and read-only — see Material)",
				"blending (decided at construction and not settable — see Material)",
				"opacity (0 to 1; nothing unless built transparent — see Material)",
				"roughness (0 to 1, 1 by default — see Material)",
				"metalness (0 to 1, 0 by default — see Material)",
				"reflectance (0 to 1, 0 by default — see Material)",
				"repeat (`[u, v]`, or one number for both; zero throws — see Material)",
				"offset (`[u, v]`: where the map starts, in whole repeats)",
				"alive (false once `dispose()` has been called on it)"
			],
			"methods": [
				"dispose()",
				"toJSON()"
			]
		},
		"ShaderMaterial": {
			"construct": "new three.ShaderMaterial({ fragment, vertex, uniforms, textures, bounds, side, transparent, blending, opacity, roughness, metalness, reflectance })",
			"note": "`fragment` is a Slang function `float3 shade(Surface s)` returning linear rgb.\n\n`Surface` carries:\n\n- `albedo`, `normal`, `uv`, `position`.\n- `color` — this copy's own, already in albedo.\n- `vertex_color` — the mesh's own COLOR_0 attribute, interpolated across the triangle, white where the file carried none, and not already in albedo. It is the one value here that varies across a surface, so it is a painted weight as often as a tint.\n- `shadow` — how much of the sun reaches this point, 1 in the open and 0 under something; 1 everywhere with shadows off, and already folded into `lambert()`.\n- `roughness`, `metalness`, `reflectance` — the material's own three, which `specular()` and `standard()` read.\n- `variant` — its row of the table, clamped.\n\nEach uniform is readable in the body by its own name; a uniform written as an array of arrays is a table column, read as `name[s.variant]`.\n\n`textures` is the same idea for images: `{ noise_map: tex }` declares a Sampler2D called `noise_map` the body samples by that name, up to eight. You never write a binding number — the shader is generated with the bindings in it and the host resolves each name through the compiled module's own reflection. Sample with any uv you like, which is the point: `s.uv + float2(t, 0)` scrolls, `s.uv * 4` tiles, `float2(k, 0.5)` reads a gradient as a lookup table. A sampler left null reads 1x1 opaque white.\n\nFive helpers are already in scope in a body:\n\n- `standard(s)` is the built-in shading, whole — `return standard(s);` draws exactly what a mesh with no ShaderMaterial draws. `standard(s, albedo, normal)` is the same with a colour and a normal you worked out yourself.\n- `lambert(normal)` is the diffuse half alone, summed over every light with the shadow folded into the sun's term — so `return s.albedo * lambert(s.normal)` is a matte surface.\n- `specular(s)` is the other half.\n- `srgb_to_linear(c)` decodes a colour you wrote down yourself.\n- `mapped_normal(s, texel)` applies a tangent-space normal map: hand it the map's rgb exactly as sampled and it answers with a world-space normal to give `lambert`.\n\n```slang\nfloat3 n = mapped_normal(s, bumps.Sample(s.uv).rgb);\nreturn s.albedo * lambert(n);\n```\n\nThe meshes here carry no tangents, so that frame is rebuilt per pixel from screen-space derivatives: it works on any textured mesh including a generated primitive, it is fragment-stage only, and it cannot see the seam of a mirrored uv island. Load the map with `{ colorSpace: three.LinearSRGBColorSpace }` — through the default sRGB the stored 0.5 that means \"no tilt\" arrives as 0.21, every surface leans the same way and the bumps go soft.\n\nIt compiles on construction, so a bad shader throws here carrying the Slang diagnostic with the line number you wrote. Needs a GPU device.\n\n`shade()` returns rgb and never alpha: how much of the surface shows is the material's opacity times this copy's `mesh.color` alpha, so a body cannot make geometry invisible by accident and a script can, deliberately. `discard` works in a body and is how a dissolve or a cutout is done.\n\n`vertex` is the other half: a Slang function `void displace(inout Vertex v)` that runs per vertex, before anything is projected. `Vertex` is the varyings — write `v.position` (world space, after the mesh's own transform) to move the vertex, and `v.normal`, `v.uv`, `v.color`, `v.vertex_color` and `v.variant` to change what the fragment stage receives; `v.local` (object space) and `v.index` (the vertex number, a per-vertex seed) are inputs only.\n\nWaves, flags, breathing, jitter, explosions, a mesh that inflates on a hit — all of them are one line here and none costs a draw call, because the geometry never changes. The normal is not recomputed from what you do to the position: write `v.normal` yourself if you moved the surface enough for the lighting to care. A sampler reads with `SampleLevel(uv, 0)` in a vertex body, not `Sample` — there are no derivatives to pick a mip with. Omitting `fragment` is allowed once `vertex` is given.\n\n`bounds` is what a vertex body owes the renderer: how far, in world units, it can move a vertex. Culling tests a mesh's own bounds, so a body that pushes geometry outside them draws something the frustum was never told about — and the symptom is geometry vanishing at the edge of the screen and coming back when the camera turns, which reads as a renderer bug. Too big costs a draw call that could have been skipped; too small drops geometry you can see.",
			"properties": [
				"uniforms (live: `mat.uniforms.tint = [1, 0, 0]`, or `mat.uniforms.palette[2] = [1, 0, 0]`)",
				"textures (live: `mat.textures.noise_map = otherTexture`, or null to put white back. Only the names given at construction exist; assigning any other throws)",
				"fragment",
				"vertex (the displace body, or an empty string; read-only, like `fragment` — a new body is a new material)",
				"bounds (how far the vertex body moves a vertex, world units; read-only, and what the frustum test is widened by)",
				"map (a `three.texture`, or null; sampled as `Surface.albedo` before your `shade()` runs)",
				"side (settable, and cheap after the first time each side is asked for)",
				"transparent (derived from `blending`, and read-only — see Material)",
				"blending (decided at construction and not settable — see Material)",
				"opacity (0 to 1; nothing unless built transparent — see Material)",
				"roughness (0 to 1, 1 by default — see Material)",
				"metalness (0 to 1, 0 by default — see Material)",
				"reflectance (0 to 1, 0 by default — see Material)",
				"repeat (`[u, v]`, or one number for both; zero throws — see Material)",
				"offset (`[u, v]`: where the map starts, in whole repeats)",
				"alive (false once `dispose()` has been called on it)"
			],
			"methods": [
				"dispose()",
				"toJSON()"
			]
		},
		"LayeredMaterial": {
			"construct": "new three.LayeredMaterial({ map, normal, mask, height, bump, layers, side, transparent, blending, opacity, roughness, metalness, reflectance })",
			"note": "An ordered stack of materials blended over a base one — terrain splatting, weathering, decals.\n\nIt is a ShaderMaterial whose `shade()` body is generated from the description, so everything a ShaderMaterial has it has, and `mat.fragment` is the Slang that was written for you — read it when a stack looks wrong.\n\nThe base material is `map` plus the mesh's own base colour, exactly as without this: the layers are extra, and a stack with none of them shades as a MeshLambertMaterial.\n\n`layers` is an array, outermost last — each is blended over everything under it as `lerp(below, blend(below, layer), mask)`. A layer takes `map` (its albedo), `normal`, `emissive`, `emissiveFactor`, `tint`, `opacity`, `roughness`, `metalness`, `metallicRoughness`, `height`, `bump`, `blend`, `mask`, `maskSource`, `maskTexture`, `invert`, `uvScale`, `uvOffset`, `enabled`, `animated` and `name`.\n\n- `mask` is which channel this layer's weight is read from — `'r'`, `'g'`, `'b'` or `'a'` — which is the economy that makes a four-layer terrain one mask image instead of four. Pass that image as the top-level `mask`. A layer with no mask covers everything; `maskTexture` gives one layer a mask of its own; `invert` flips it.\n- `maskSource` says which thing the channel belongs to: `'texture'` (the default) or `'vertexColor'`, which reads the mesh's own COLOR_0 attribute — a weight an artist painted per vertex, costing no sampler and no image at all.\n- A layer that states no colour — no map, no tint, not animated — leaves what is under it alone rather than blending white over it, so `{ emissive: glow }` only glows and `{ normal: bumps }` only adds detail. Use a white map to paint white deliberately.\n- `blend` is `'mix'` (the default), `'multiply'`, `'add'`, `'subtract'`, `'screen'`, `'overlay'`, `'softLight'`, `'difference'`, `'darken'` or `'lighten'` — Blender's Mix node modes, because that is where the glTF extension this implements comes from.\n- `uvScale` is per layer and tiles the detail without tiling the mask, which is the whole trick of a splat map. It composes with `material.repeat` rather than replacing it.\n\nEverything is baked into the shader as a literal unless you say `animated: true` on a layer, which promotes its `tint` and `opacity` to a uniform you can write every frame — `mat.layers[2].opacity = 0.25`. That costs 16 of the material's 104 uniform bytes, so at most six layers may be animated; the rest cost the push block nothing.\n\nThe real ceiling is samplers: eight, counting one per layer `map`, `normal`, `emissive`, `height`, `metallicRoughness` and own `mask`, plus one each for the shared mask and the base normal and height. The base map does not count. `{ enabled: false }` drops a layer and its samplers entirely.\n\nLoad masks and normal maps with `{ colorSpace: three.LinearSRGBColorSpace }` — their channels are numbers rather than colours, and through the default sRGB every weight comes out wrong.\n\nA layer may change the surface, not just its colour: `roughness` and `metalness` are 0-to-1 numbers and `metallicRoughness` is a map packed glTF's way (green rough, blue metal). They blend down the stack by the same weight and the same blend mode the colour does, so a moss layer that multiplies its colour also multiplies its roughness. State only what the layer means — a layer that says `roughness` says nothing about `metalness`, and the material's own value carries through.\n\n`height` is parallax: `bump: { strength, distance }` scales it, `distance` in metres, and the uv moves under everything sampled after it. A height on the LayeredMaterial itself moves the whole stack — base colour, base normal, the mask and every layer; a height on a layer moves that layer's own maps and nothing else. 0.5 in the map is the plane the mesh already has. It is one step rather than a march, so it shifts convincingly and does not occlude, and it does nothing in a bake.\n\nA layer's `subsurface` is the one thing refused rather than ignored: it needs a light transport this renderer does not have, and a material property that provably changes no pixel is worse than an error.\n\n`asset.mesh(name).layers` hands you a description straight out of a glTF authored with `CUSTOM_materials_layers`, so `new three.LayeredMaterial(ref.layers)` is the whole import.",
			"properties": [
				"layers (a view per enabled layer: `layers[i].map = tex` swaps an image, and `layers[i].tint` / `layers[i].opacity` read and write the ones declared animated)",
				"fragment (the generated Slang — read-only, and the thing to look at first)",
				"uniforms, textures (the ShaderMaterial proxies, under the generated names)",
				"map, side, transparent, blending, opacity, roughness, metalness, reflectance, repeat, offset, alive (as ShaderMaterial)"
			],
			"methods": [
				"dispose()",
				"toJSON()"
			]
		},
		"Group": {
			"construct": "new three.Group(), or asset.instantiate()",
			"note": "Transforms its children and draws nothing itself, which makes it the way to keep several objects one object: parent the pieces, place them relative to the Group once, and afterwards there is one transform to move rather than a convention to remember.\n\n`asset.instantiate()` answers with one of these carrying the file's own node hierarchy, and that one is what `animations`, `play(name, {loop, speed, time, fade})` and `stop()` work on — a glTF clip drives a whole subtree, so its root is where it is played. On a hand-built Group `animations` is empty and `play()` throws saying which door to use.\n\nThere is no AnimationMixer: one clip at a time plus a crossfade. `fade` is seconds to blend out of whatever is playing, and asking to fade into the clip already playing does nothing — which is what makes `play(state.clip, { fade: 0.2 })` safe to call from a state machine every frame. Restarting a clip outright is `play()` without a fade.",
			"properties": [
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"children",
				"parent",
				"static (this will not move again — see the static-casters topic)",
				"collides (per node and not inherited; set it on the meshes, not on the Group)",
				"animations (clip names, from `asset.instantiate()`)"
			],
			"methods": [
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		},
		"Box3": {
			"construct": "new three.Box3(minX, minY, minZ, maxX, maxY, maxZ)",
			"note": "An axis-aligned box, and the answer to \"how big is this actually\". A kit piece's origin is wherever whoever exported it left it, so nothing about a transform says where the piece's faces are — which is what \"put this window on that wall\" is really asking.\n\n`size` and `center` are derived from `min`/`max` rather than stored. `edge(axis, which)` is one face's coordinate, and is what `align()` is written in terms of.\n\n`containsPoint` is the trigger test that costs no host call — a box out of `object.boundingBox()` against a position a script already has is the cheapest volume there is, and `three.physics` triggers are what to use when the volume has to move. `intersectsBox` counts touching as overlapping, as `three.query.box` does. `expandByScalar` answers with a new box, so it does not disturb the one it grew from.",
			"properties": [
				"min",
				"max",
				"size",
				"center"
			],
			"methods": [
				"edge(axis, 'min' | 'center' | 'max')",
				"containsPoint(point)",
				"intersectsBox(other)",
				"expandByScalar(amount)",
				"union(other)",
				"clone()",
				"toJSON()",
				"toString()"
			]
		},
		"MeshRef": {
			"construct": "not constructible — asset.mesh(name) and asset.meshAt(i) answer with these",
			"note": "One piece of a loaded file: the handle `new three.Mesh()` wants, plus bounds.\n\nReading `bounds` costs no upload — the box comes out of the glTF JSON at load, so asking how big two hundred kit pieces are before placing twelve of them still uploads twelve. It is not cached: a reference that outlives its asset throws rather than answering with the size the mesh used to be.",
			"properties": [
				"asset",
				"assetGeneration",
				"mesh",
				"name",
				"bounds (a Box3 in the mesh's own space)",
				"layers (this mesh's `CUSTOM_materials_layers` stack as a LayeredMaterial description, or null)",
				"material (what this primitive's glTF material said, beyond the base colour, or null)"
			],
			"methods": [
				"split() (cut this mesh into its connected components, one geometry per piece)",
				"toJSON()",
				"toString()"
			],
			"details": {
				"layers": "`new three.LayeredMaterial(ref.layers)` is the whole import, and what you get first is a plain object you may edit — drop a layer, retune an opacity, mark one animated.\n\nUnlike everything else on a MeshRef this uploads the mesh, because a stack is texture slots and slots exist only once the primitive is on the device. Every read hands back fresh Texture handles each holding a reference, so read it once and keep what it gave you.",
				"material": "`{ alphaMode, alphaCutoff, doubleSided, normalMap, emissive, emissiveMap, emissiveIntensity, aoMap, metalness, roughness, metalnessRoughnessMap }`, or null for a primitive that names no material. This is what the loader used to drop.\n\nEach map arrives with its colourspace already right — normal, occlusion and metallic-roughness are data and load linear, emissive is a colour and loads sRGB — which is decided by the importer rather than by you, and is the difference between a normal map that works and one that leans every surface the same way.\n\nIt is a description and not a material: what to build from it is yours, and `asset.instantiate({ materials: true })` is the shorter door.\n\n`aoMap` has no built-in term (one line in a shade body multiplies by it) and `metalnessRoughnessMap` has none either, because this renderer's pair is per material and a map of them is per texel — a LayeredMaterial layer's `metallicRoughness` is where a map of them does something. `metalness` and `roughness` themselves are the file's numbers and `instantiate()` applies them; glTF defaults both to 1, so a file that says nothing is fully metallic and is dark without a sky to reflect.\n\nLike `layers`, this uploads the mesh and every read hands back fresh Texture handles holding references, so read it once and keep it.",
				"split": "The answer to a kit that arrived as one merged mesh: a town square with 23 buildings in it, or a pack of four animals, is one transform and one bounding box until it is cut, so nothing in it can be placed, rotated, culled or picked on its own.\n\nTwo triangles are in the same piece when they share a vertex, which is the right cut for a merged kit (they are merged by concatenation, so nothing welds them) and no cut at all for a surface that is genuinely connected — a terrain with the houses extruded out of it comes back whole. Length one means it was already one thing, and the one geometry you get is this mesh itself with nothing uploaded.\n\nEach piece is an ordinary geometry: instanced, pickable, exportable, unloadable on its own, carrying the source's colour and base colour map. It does not carry a layer stack, and a mesh that has one throws instead of losing it quietly.\n\nExpect the pack to include details that were modelled as separate shells — eyes, horns, hooves — so filter by `piece.bounds.size` if you only want the bodies.\n\nNot free and not automatic: it reads the geometry back and uploads one asset per piece, so it is a load-time step. Splitting the same mesh twice answers with the same assets."
			}
		},
		"Vector3": {
			"construct": "new three.Vector3(null, x, y, z)",
			"note": "`position`/`rotation`/`scale` are live Vector3s: writing `x`, `y`, `z` or calling `set()` moves the object.\n\nEvery method below except the read-only ones mutates and answers with `this`, which is Three.js's convention and the one thing worth saying out loud: `a.cross(b)` does not mean \"the cross product of a and b\", it means \"a becomes the cross product\". On a live vector that is a write to the object, so `dir.copy(mesh.position).sub(target).normalize()` moves the mesh and `mesh.position.clone().sub(target).normalize()` is what was meant.\n\nThe ones that only read are `dot`, `length`, `lengthSq`, `distanceTo`, `distanceToSquared`, `angleTo`, `equals`, `clone` and `toArray`.\n\n`normalize()` leaves a zero vector at zero rather than handing back three NaNs, because a direction is almost always fed straight to something that aims, and a NaN aim renders as the object vanishing rather than as an error.\n\nIn a loop compare `distanceToSquared` against `r * r`: `distanceTo` is a square root per pair to answer a question that never needed one.",
			"methods": [
				"set(x,y,z)",
				"copy(v)",
				"add(v)",
				"sub(v)",
				"multiplyScalar(s)",
				"divideScalar(s)",
				"addScaledVector(v,s)",
				"setScalar(s)",
				"negate()",
				"lerp(v,t)",
				"cross(v)",
				"normalize()",
				"dot(v)",
				"length()",
				"lengthSq()",
				"distanceTo(v)",
				"distanceToSquared(v)",
				"angleTo(v)",
				"equals(v)",
				"clone()",
				"toArray()",
				"toJSON()",
				"toString()"
			]
		},
		"Random": {
			"construct": "new three.Random(seed)",
			"note": "A seeded generator. It exists because `Math.random` throws away the determinism the rest of the engine goes to some trouble to have: the fixed step, the solver's own accumulator and `state_hash` are all so that the same inputs give the same frame, and one `Math.random()` in the gameplay layer costs all of it — a bug that reproduces on the tester's machine and not on yours, with no way to bisect.\n\n`three.randFloat`, `three.randInt` and `three.randFloatSpread` are Three.js's names drawing from a shared stream that `three.seed(n)` resets. Construct one of these when two systems must not perturb each other's sequence, because a level generator and a particle burst drawing from one stream means adding a spark changes the terrain.\n\nmulberry32 — fast, small, and not cryptographic. `seed(0)` is replaced, because a zero state is this generator's one short cycle. `int(low, high)` is inclusive at both ends, as Three.js's `randInt` is.",
			"methods": [
				"float()",
				"range(low,high)",
				"int(low,high)",
				"spread(range)",
				"chance(p)",
				"pick(list)",
				"sign()",
				"seed(value)",
				"toString()"
			]
		},
		"Asset": {
			"construct": "three.load(path)",
			"note": "A parsed `.glb` or `.gltf`. Two doors onto it: `instantiate()` for the file's own node hierarchy, and `mesh(name)` for one piece you place yourself.\n\n`instantiate()` is Three.js's `gltf.scene` — the file's nodes as Object3Ds, with the transforms the file gave them. Use it for anything whose pieces are positioned by nodes rather than baked into the vertices: a rig, a prop with parts, a level laid out in Blender. Instantiating twice gives two independent trees over one upload.\n\n`{ materials: true }` builds a material per glTF material and puts it on the meshes that wear it, which is how a `.glb` authored with `alphaMode BLEND` renders blended and how a file's normal maps and emissive maps reach the frame. Without it the file draws with its base colour and base colour map and nothing else. It builds nothing for a material that is opaque, single-sided and has no normal or emissive map, because that is the default material already. Occlusion and metallic-roughness are not applied; `ref.material` has them and the reason. Off by default because it compiles a shader per distinct material.\n\nFor a rigged file, the skeleton is left out by default and the character is posed from a table baked once at load, so a hundred of them is a hundred nodes, one draw call and one uint per copy per frame — give each a phase with `play(name, { time })`.\n\n- `{ skeleton: true }` keeps the bones as objects and switches that copy onto a palette computed from them every frame, so writing `bone.rotation` moves the skin — a look-at, an aim, a foot on a slope. That is the hero-character option and it costs per copy.\n- `{ skinning: 'compute' }` poses the vertices in a compute pass instead of in the vertex shader. It splits the character into its own draw call and holds a posed copy of the mesh, and only pays off when the same character is drawn more than once a frame.\n\n`asset.imageAt(i)` is the file's own pictures as ordinary Textures, numbered the way the glTF numbers them and counted by `asset.images`. It answers the same slot a placed mesh is drawing with, so what you read is what is on screen, and it decodes on demand if nothing has placed a mesh yet. It takes the same options `three.texture` does; sRGB is the default and is wrong for a normal or roughness map. Null comes back for an image nothing here decodes.\n\n`meshAsync` / `meshAtAsync` / `instantiateAsync` are the same three verbs with the upload awaited. A `.glb` is parsed at load and its meshes reach the device one at a time when something first draws each, so `scene.add` of a ninety-piece kit does ninety uploads inside one frame and that frame hitches. Awaiting hands the engine a queue it drains one mesh per frame, so the kit arrives over ninety frames with the game still drawing. Per mesh and not per file, because a level needs its floor before its ninetieth crate. In a one-shot script with no animation loop there is no frame to protect and it costs what the synchronous path costs. They reject if the asset is unloaded before their turn comes up.",
			"properties": [
				"path",
				"meshes (names, in load order)",
				"animations (clip names)",
				"images (how many pictures the file holds)",
				"bones (the rig's joint names — what `socket(name)` takes. Empty for a file with no skin)"
			],
			"methods": [
				"mesh(name)",
				"meshAt(index)",
				"imageAt(index, { colorSpace, generateMipmaps })",
				"meshAsync(name)",
				"meshAtAsync(index)",
				"instantiate(name?, { skeleton, skinning, materials })",
				"instantiateAsync(name?, { skeleton, skinning, materials })",
				"toJSON()"
			]
		},
		"Sockets": {
			"construct": "character.socket(boneName)",
			"note": "Put the sword in the hand. `socket(name)` answers with an object parented to a bone of an instantiated character — `add()` things to it and they ride the animation.\n\n`asset.bones` is the list of names to pass, and it has to be: a rig calls its hand `mixamorig:RightHand` or `hand.R` or `Bone.014` depending on who exported it, and the names are not guessable.\n\nThe two kinds of character answer differently, and that is the point. With `{ skeleton: true }` the bones already are objects in the tree, so `socket()` hands back the bone itself. A baked character has no bone objects at all — dropping them is what makes a hundred of them a hundred nodes — so it makes a holder and the engine keeps it on the bone, reading the transform out of the same pose table the character is drawn from. Either way what comes back is something you can `add()` to, place relative to, and remove.\n\nIt costs a second copy of the file's pose table, once per asset and only from the first socket on it — so a crowd nobody attaches anything to pays nothing. A socket on a character standing still costs nothing per frame either; it is rewritten only when the pose changes.\n\nAsking for a bone the rig has not throws, with the list of the ones it has.",
			"methods": [
				"socket(boneName)"
			]
		},
		"Geometry": {
			"construct": "not constructible — use one of the shapes below",
			"note": "What every shape is: a handle three.c3 built, carrying the numbers you asked for. Hand it to `new three.Mesh()`.\n\nConstructing the same shape twice answers with the same asset, so a geometry per mesh costs nothing and a thousand identical ones are one draw call; two different sizes are two.\n\nThere is no BufferGeometry and no attribute access — a script describes shapes, never vertices, and ConvexGeometry's point cloud is a description too.\n\nSizes are world units and must be positive, segment counts are capped at 512, Y is up, and every shape is centred on its own origin.",
			"properties": [
				"type",
				"name",
				"parameters (what you asked for, defaults filled in)",
				"asset",
				"mesh",
				"bounds (a Box3 in the shape's own space — what it is, which is not always what it was asked for)"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"BoxGeometry": {
			"construct": "new three.BoxGeometry(width = 1, height = 1, depth = 1, widthSegments = 1, heightSegments = 1, depthSegments = 1)",
			"note": "A box centred on the origin. The segment counts subdivide it and change nothing about its size.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"SphereGeometry": {
			"construct": "new three.SphereGeometry(radius = 1, widthSegments = 32, heightSegments = 16)",
			"note": "A UV sphere with its poles on the Y axis.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"PlaneGeometry": {
			"construct": "new three.PlaneGeometry(width = 1, height = 1, widthSegments = 1, heightSegments = 1)",
			"note": "A one-sided rectangle in the XY plane, facing +Z — Three.js's orientation, which is vertical. A floor is this with `rotation.x = -Math.PI / 2`. From behind it is invisible, because back faces are culled.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"CylinderGeometry": {
			"construct": "new three.CylinderGeometry(radiusTop = 1, radiusBottom = 1, height = 1, radialSegments = 32, heightSegments = 1, openEnded = false)",
			"note": "A cylinder or a truncated cone about the Y axis. Either radius may be 0, but not both.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"ConeGeometry": {
			"construct": "new three.ConeGeometry(radius = 1, height = 1, radialSegments = 32, heightSegments = 1, openEnded = false)",
			"note": "A cone about the Y axis with its point up. The same triangles as `CylinderGeometry(0, radius, height)` — and the same asset, so the two spellings share a draw call.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"TorusGeometry": {
			"construct": "new three.TorusGeometry(radius = 1, tube = 0.4, radialSegments = 12, tubularSegments = 48)",
			"note": "A ring in the XY plane. `radius` is measured to the centre of the tube, so the shape is `2 * (radius + tube)` across and `2 * tube` thick.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"ConvexGeometry": {
			"construct": "new three.ConvexGeometry(points)",
			"note": "The convex hull of a cloud of points, and the way to make a shape that is not one of the six parametric ones — a rock, a crystal, a gem, a chunk of debris, the bound of a scan.\n\n`points` is an array of Vector3s, of `[x, y, z]` or of `{x, y, z}`, or a flat array or Float32Array of coordinates; at least 4 points, at most 65536.\n\nThe hull is flat shaded, because its faces meet at hard creases. It carries uvs, but they are a projection rather than an unwrap: every facet is mapped face-on at one uv unit per unit of local space, so a texture is never stretched and is the same size everywhere on the hull and on anything beside it. The only artefact is that two facets meeting at a crease do not line up along the shared edge, which reads as nothing on the noise and grain a rock wants and as a break on a regular pattern.\n\nThe points describe the shape, they are not its vertices — most are discarded and none can be read back. `parameters.points` is the count you handed over. Two identical arrays are one asset; two runs of `Math.random()` are two, because the key is bit-exact.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"TerrainGeometry": {
			"construct": "new three.TerrainGeometry({ width, depth, segments, heights, skirt })",
			"note": "Ground that is a surface rather than a pile of boxes, and the only geometry here that answers questions afterwards.\n\n`heights` is a `three.Field`, a flat array of `(segments + 1)` squared numbers row-major in z, or a function `(x, z) => y` sampled at the grid points in world coordinates — the same frame everything else in the scene uses, so one height function can drive the terrain, the mask and the scatter. Omit it for a flat field to stamp into later.\n\nIt lies in the xz plane with +y up and is centred on the origin, its uv runs 0..1 across the whole field so a splat mask lines up with no transform, and its normals come from the grid rather than from the triangles — which is the difference between ground and steps. `skirt` is how far a wall drops around the border so the map edge is not a hole.\n\nOne asset, one draw call: a 256-segment field is one `vkCmdDrawIndexed`.\n\n`heightAt(x, z)` and `normalAt(x, z)` read the same grid the mesh was built from, through the same interpolation, so what a script stands on cannot disagree with what is drawn. Off the map the edges extend outwards rather than dropping to zero.",
			"properties": [
				"bounds"
			],
			"methods": [
				"heightAt(x, z)",
				"normalAt(x, z)",
				"toJSON()",
				"toString()"
			]
		},
		"RibbonGeometry": {
			"construct": "new three.RibbonGeometry({ path, width, y, terrain, lift, samples, columns })",
			"note": "A mesh that follows a curve — a road, a river, a path, a wall — and the answer to \"why does the bend look like it was drawn with a ruler\".\n\n`path` is sparse control points (`[[x, z], ...]` or `{x, z}`) and is bent by the same centripetal Catmull-Rom as `three.catmullRom`, sampling `samples` cross-sections per control segment, so you write the bends you can see and the ribbon is smooth between them.\n\nTwo modes, by the options:\n\n- Flat at a constant `y` — a river or a pond, since a water surface is a plane and not a drape.\n- Draped over `terrain` (a TerrainGeometry or a Field), where every vertex is `heightAt`/`valueAt` plus `lift` so the strip hugs the ground. `lift` keeps it from z-fighting where it lies.\n\n`columns` is the cross-section: 2 is a straight chord across the width, more follows a crown or a bank. `width` is the full width in world units; u runs across it and v along the length, so a texture flows with the road.\n\nOne asset, one draw call — the strip is one mesh, not a row of re-edged boxes.",
			"properties": [
				"bounds"
			],
			"methods": [
				"toJSON()",
				"toString()"
			]
		},
		"Field": {
			"construct": "new three.Field({ width, depth, segments, value })",
			"note": "A scalar grid in world coordinates — the authoring half of TerrainGeometry, and the same object a splat mask is made of.\n\nThat is the point rather than a convenience: carve a river channel and stroke the mud mask from one polyline, and the mud is where the water is by construction instead of because two functions were kept in step.\n\nEverything mutates in place and returns `this`, so it chains.\n\n- `fill` and `add` take a number or an `(x, z) => v` in world coordinates.\n- `flatten({x, z, width, depth}, y)` is a building pad, defaulting `y` to the mean under the rect — which is where the ground already was.\n- `carve(path, width, depth)` lowers along a polyline and `stroke(path, width, value)` paints along the same one.\n- `circle`, `blur`, `normalize` and `clamp` finish the set.\n\nEvery stamp takes a feather in world units with a smoothstep falloff.\n\n`valueAt(x, z)` reads it back bilinearly before any upload, which is what lets a script place buildings and scatter trees on ground that does not exist on the device yet.\n\n`texture()` is the field as a mask in all four channels; `three.Field.mask({r, g, b, a})` packs up to four of one resolution into the RGBA image a LayeredMaterial reads, always linear because a mask is a weight and not a colour.",
			"properties": [
				"width",
				"depth",
				"segments",
				"side",
				"values"
			],
			"methods": [
				"fill(v)",
				"add(v)",
				"flatten(rect, y, feather)",
				"carve(path, width, depth, feather)",
				"stroke(path, width, value, feather)",
				"circle(x, z, radius, value, feather)",
				"blur(passes)",
				"normalize(low, high)",
				"clamp(low, high)",
				"range()",
				"valueAt(x, z)",
				"xAt(i)",
				"zAt(j)",
				"texture()"
			]
		},
		"CatmullRomCurve3": {
			"construct": "new three.CatmullRomCurve3(points, closed, curveType, tension)",
			"note": "The three-dimensional half of the curve pair, and the one a loop samples rather than a bake consumes: a camera rail, a patrol route, a projectile arc, a rope. Three.js's class and method names.\n\n`curveType` is `'centripetal'` (the default), `'chordal'` or `'uniform'` — centripetal passes through every control point without swinging wide of a tight one, which is what a hand-written path always has.\n\n**`getPoint(t)` and `getPointAt(u)` are not the same function**, and the gap between them is what makes hand-written rail code look wrong: `t` is the curve's own parameter, spread evenly over the control segments, so an object moving at a constant `t` per second speeds up through the widely spaced ones and crawls through the close ones. `u` is spread evenly over the length, and that is the one anything moving wants.\n\n`three.catmullRom` is the other half of the pair: a ground path, `[x, z]` in and a dense polyline out, for `field.carve`, `field.stroke` and RibbonGeometry.",
			"properties": [
				"points",
				"closed",
				"curveType",
				"tension"
			],
			"methods": [
				"getPoint(t)",
				"getPointAt(u)",
				"getTangent(t)",
				"getLength()",
				"getPoints(count)",
				"getSpacedPoints(count)",
				"toString()"
			]
		},
		"QueryResult": {
			"construct": "three.query.buffer(capacity)",
			"note": "A reusable answer for the flat form of `three.query.sphere` and `three.query.box`. Make it once, outside the loop: the whole reason it exists is that a query answering with an Array of objects allocates, and a hundred agents asking every frame is a hundred allocations a frame.\n\n`handles` is the raw Int32Array of index/generation pairs the host filled, so a script that only wants to count what is nearby never resolves anything at all; `objects()` resolves the lot in one walk of the scene when it does.\n\n`full` is true when the query filled the buffer, which means there may be more it could not tell you about — a count equal to the capacity is otherwise indistinguishable from a scene that happened to have exactly that many.",
			"properties": [
				"handles",
				"capacity",
				"count",
				"full"
			],
			"methods": [
				"objects()",
				"toString()"
			]
		},
		"Entity": {
			"construct": "class Critter extends three.Entity { ... }, then Critter.spawn(...)",
			"note": "An entity is a class, and this is the base to extend. There is no registration call beside the declaration: the class registers itself on first use, reading `static capacity`, `columns`, `parent`, `body`, `volume`, `trigger`, `collides` and `name` off itself.\n\n`super()` first in the constructor, and it is not a formality — that is where a bare `new Critter()` is refused, which is the one check a class with no columns cannot get any other way. An untracked instance has no node in the scene, no body in the solver and no place in the live list, and the first sign of it is a thing that never appears.\n\n`Critter.step(name, fn)` and `Critter.frame(name, fn)` register a system under this class's name, so a report says `Critter.walk` rather than `walk` — the same two verbs as `three.systems`, with `system(name, fn, options)` leaving the phase open.\n\nThere is no `update()` to override, on purpose: continuous work is a named system in a readable order, and a per-entity update method is the ninety-line animation callback again, once per class. `onSpawn()` and `onRemove()` are the hooks there are.",
			"properties": [
				"count",
				"free",
				"capacity",
				"trackName",
				"handles",
				"transform"
			],
			"methods": [
				"spawn(...args)",
				"of(object)",
				"remove(instance)",
				"column(field)",
				"all()",
				"compact()",
				"clear()",
				"dispose()",
				"on(event, matcher, fn, options)",
				"off(name)",
				"step(name, fn, options)",
				"frame(name, fn, options)",
				"system(name, fn, options)",
				"pose(field, options)",
				"flush()",
				"sync()"
			],
			"details": {
				"column": "Columns are a window, not a copy. `static columns = { position: 3 }` with a `capacity` gives each instance a subarray over one shared Float32Array, so `this.position[1]` and `Critter.column('position')` are the same memory, and `three.steer` / `field.sample` / `three.moveAndSlideAll` are handed the storage itself with nothing gathered.\n\nDeclare only the fields a bulk verb reads. `hp`, `stun` and `heading` are ordinary properties, because nothing takes those in bulk. Measured, so it is a choice and not a rule: the copying alternative costs a flat 115-148 ns per entity, which is 0.02 per cent of a frame at ten and 1.4 per cent at a thousand. Declare columns in the low thousands and not before.\n\nA column cannot grow, because every live window into it would dangle — which is why a capacity is required beside them and why a full class throws rather than reallocating.\n\nA columned field has a getter and no setter: `c.position[1] = 5` writes the column and `c.position = [0, 5, 0]` throws, because swapping the window for a plain array would silently stop that entity steering.",
				"remove": "A removal is immediate and the list compacts at the end of the frame.\n\n`c.remove()` takes the body, the volume and the node away and makes `of()` answer null before it returns, because a crate broken inside a spin has to stop colliding and stop drawing on this tick. Only the live list waits, which is what makes `for (const c of Critter)` safe to remove from.",
				"compact": "An internal system named `<Class>.compact` closes the gaps last in the frame phase, moving the column floats down and re-numbering the slots. A view is a function of the slot, so nothing has to be re-seated — but a subarray held across a frame boundary points at whatever moved into that slot.\n\nCompaction is stable rather than a swap-remove: a crowd that reorders itself whenever something dies makes `three.steer`'s separation, which reads neighbours out of the same array, behave differently for reasons nothing in the game can see.",
				"of": "Resolves up the parent chain, so a raycast hitting one mesh of an assembled eleven-part character answers with the instance, and so does a volume.",
				"pose": "`pose(field, { lift, heading })` copies a 3-float column into the transform with a vertical offset — the capsule centre and the model origin are never the same point — and `heading` may be a column or an ordinary field.",
				"flush": "Sends the transform to the nodes in one crossing. `handles` is both the `self` column `three.moveAndSlideAll` takes and the handle array this uses.",
				"sync": "Copies the transform back onto the objects, and carries the same trap `three.batch` does: a flush writes the node, so the objects go stale and writing any single component of one afterwards undoes the flush for that entity."
			}
		},
		"Widget": {
			"construct": "class Hud extends three.Widget { render() { ... } }, then Hud.mount()",
			"note": "A widget is a class and this is the base to extend — the interface written the way an immediate-mode one is, costing what a retained one does.\n\n`render()` describes the interface as it is now out of the node classes on `three.ui`; assigning a field marks the widget for a re-render; and what reaches the host is the difference — one `three.ui.patch` per changed value, and a `set` only when the shape itself changed.\n\nSo the two things `three.ui.set`/`patch` make a script carry stop existing: a unique string key per value invented by hand, and the rule that every tree the game can set must still contain the keys the loop patches.\n\nThe nodes are objects, one class per kind, on `three.ui`: Column, Row, Stack, Padding, Grid, Clip, Anchored, Scroll, Panel, Rect, Label, Drawing, Button, Checkbox, Slider, Select, Tree, TextField. Destructure them in one line — `const { Panel, Label, Button } = three.ui`.\n\nArguments are read by type, so the common spellings need no property names: a string is the text, a function is the handler, a boolean is `checked`, a number is the value, an array is the options or the rows, a plain object is the rest of the properties, and anything else is a child.\n\n```js\nnew Button('Reset', () => reset());\nnew Checkbox('Wireframe', on, v => set(v));\n```\n\nChildren are the arguments after the property bag rather than a `children` array, and a falsy one is skipped, so `cond && new Row(...)` is a conditional row.\n\nPanel is the one composite and it is the one that stops the arithmetic: a background behind a padded column, sized to what is in it. A Stack fills, which is right for a scrim and wrong for a card, so a card written by hand has to be told its height — measure a line, add up the rows, add the insets — and that number stops being true the moment a row is added. `{ at: 'top-left', margin: 16 }` anchors it in the frame, and `width` fixes the axis that should not hug.\n\nKeys are paths. Every node is keyed by its position in its widget's tree, prefixed by the widget's own id, so nothing is named by hand and two widgets cannot collide. Give a key by hand where a list reorders — the same reason React wants one — and the text under somebody's fingers follows the row rather than the position.\n\nThe invalidation is a Proxy: the constructor returns one, so every write to every field through every reference anything captured marks the widget dirty. Its one blind spot is a field mutated rather than assigned — `this.rows.push(x)` — and `update()` is the escape hatch for exactly that. A handler firing marks it dirty too. The re-render runs once a frame in a system named `ui.widgets`; `three.ui.flush()` is it now, for a screenshot taken without drawing a frame first.\n\nMounted widgets are floors of one interface, drawn in static layer order then mount order — a HUD and a pause screen over it are two classes, and unmounting one leaves the other standing rather than rebuilding it. An unmounted widget is a legitimate thing: `new Bar()` as a child of another's `render()` has its own state and marks its owner dirty, which is how a row of stats is written once and used four times.\n\n`three.ui.set` and `three.ui.draw` are refused while anything is mounted, because they are the other door onto one interface and the class layer would overwrite them on the next frame. `three.ui.clear()` and `three.Widget.unmountAll()` are how the class layer goes away. `onMount()` and `onUnmount()` are the hooks there are.",
			"properties": [
				"isMounted",
				"count",
				"layer"
			],
			"methods": [
				"mount()",
				"unmount()",
				"update()",
				"render()",
				"mount(...args)",
				"all()",
				"unmountAll()"
			]
		},
		"Cooldown": {
			"construct": "three.cooldown(duration, options)",
			"note": "A scalar gameplay timer — the player's spin, a hurt window, coyote time — for the `if (x > 0) x -= dt` pattern written out by hand at every one of those call sites.\n\nTicked, not read off `three.clock`: `three.clock.time` only advances once per host tick, before the fixed loop decides how many steps it owes that tick, so a window shorter than one fixed step would see zero elapsed time across a multi-step catch-up frame. A Cooldown instead sits in a module-level list a lazily-registered `three.systems` entry decrements every step — which is also why a paused `three.clock` freezes it for free.\n\n`duration` is seconds greater than zero. Options are `{ recover, phase }`: `recover` is seconds after `active` ends before `ready` is true again (0 by default), and `phase` is `'fixed'` (the default) or `'frame'`.\n\n`start(options)` starts it if ready and answers whether it did. `{ restart: true }` starts it even mid-active or mid-recovering — coyote time re-armed every grounded frame, or a buff refreshed by a second pickup — and a refused start changes nothing. `cancel()` ends it now and skips recovery: \"the spin was interrupted\", not \"the spin finished early\".\n\n`duration` and `recover` are ordinary writable properties and every getter reads them fresh, so upgrading the spin needs no re-`start()`. `dispose()` takes it out of its phase's tick list, and takes the phase's system back out of `three.systems` the moment nothing else needs it.",
			"properties": [
				"duration",
				"recover",
				"started",
				"active",
				"ready",
				"recovering",
				"remaining",
				"progress",
				"elapsed"
			],
			"methods": [
				"start(options)",
				"cancel()",
				"dispose()",
				"toString()"
			]
		},
		"TransformBatch": {
			"construct": "three.batch(objects, { trs })  |  three.batch(objects, { euler: true })",
			"note": "A Float32Array-shaped bulk write over many nodes.\n\n`positions` is three floats per object, seeded from where they are now, so a batch made and immediately flushed changes nothing. `flush()` sends the lot in one crossing and answers with how many landed; a member that has left the scene is skipped rather than throwing, because a crowd where one agent was removed this frame is ordinary.\n\nTwo rotation forms, and they are not redundant:\n\n- `{ trs: true }` is a stride of ten — position, an xyzw quaternion, then scale — for a batch written by arithmetic, where whatever produced the rotation (a look-at, a slerp, a physics read-back) produced a quaternion, and converting it to Euler to send it would be lossy at every gimbal-locked pose.\n- `{ euler: true }` is a stride of nine — position, an xyz Euler triple, then scale — for a batch written by a game, where the rotation is a heading and a limb swing: one angle each, typed by a person. This is what makes a crowd of characters one crossing instead of four: a critter writing a group position, a group heading and two leg angles measured 1.48 µs a frame as four ordinary writes and 405 ns through one batch.\n\nThey also seed differently, for the same reason. A TRS batch starts at an identity rotation and a unit scale, because reading the objects' Euler angles and converting them would silently rewrite rotations the script set by hand; an Euler batch is the script's own numbers and is seeded from `object.rotation` and `object.scale`. `rotationAt(i)` and `scaleAt(i)` give the index those start at, so `i * stride + 3` is never written out by hand.\n\nFlush writes the node, and the object stops agreeing with it. `object.position` and `object.rotation` are JavaScript numbers the host is never the authority on, and a batch goes straight to the node — so reading `object.position.x` back gives the stale value, and worse, writing any single component afterwards undoes the batch, because `object.position.y = 5` sends all nine of the object's numbers and overwrites the rotation and scale the batch wrote. It renders as a crowd snapping back to a pose it had frames ago.\n\n`sync()` is the fix and it is opt-in: it copies the array back onto the objects in JavaScript with no crossing. Call it when a script is about to touch those objects by hand again, and not every frame merely because it is there. `boundingBox()`, `align()` and the follow camera all read the host and are unaffected.\n\nIt is not a faster way to move a dozen things — five hundred ordinary `mesh.position.set` calls are 0.245 ms, three per cent of a frame. What it is for is the case where the write is already a loop over numbers: a crowd steered by `three.steer`, a particle field, a chunked terrain.",
			"properties": [
				"positions",
				"data",
				"handles",
				"objects",
				"trs",
				"euler",
				"mode",
				"stride",
				"length"
			],
			"methods": [
				"flush()",
				"sync()",
				"rotationAt(i)",
				"scaleAt(i)"
			]
		},
		"NavField": {
			"construct": "three.nav.field(goals)",
			"note": "A solved flow field over the current `three.nav.bake()` — the solve kept, which is the whole reason there are two navigation verbs instead of one. `three.nav.path` solves the entire reachable set and throws it away after one answer, so a hundred agents heading for the same door is a hundred solves for one field.\n\n`direction(point)` is a unit XZ vector, or zero for nowhere to go — standing on the goal, off the mesh, or in a pocket no goal reaches — and `cost(point)` is how those are told apart. Cost is measured along the ground rather than through walls, and is Infinity for unreachable.\n\nFeed the field to `three.steer` rather than calling `direction()` in a loop: that is one crossing for the whole crowd instead of one per agent.\n\nFreed by `dispose()` and by disposing the scene it belongs to, and nothing else — a field is one float per walkable cell and this API does not collect.",
			"properties": [
				"alive"
			],
			"methods": [
				"direction(point)",
				"cost(point)",
				"reaches(point)",
				"sample(positions, { costs, directions })",
				"dispose()",
				"toString()"
			],
			"details": {
				"sample": "Direction and cost for a whole crowd in one crossing, and the reason `direction` and `cost` are the wrong verbs to call in a loop — not because of the crossing but because of the argument checking in front of it. The bare host call that answers a number measures 143 ns; `cost(point)`, the same call through the `readVector` that allocates a three-element array and runs three `Number.isFinite` checks to be polite about its argument, measures 652 ns; `sample` measures 159 ns an agent.\n\n`positions` is three floats per agent and is read. `costs` is one float per agent and `directions` is three, and either may be left out, so a caller asking only how far everyone is pays for no directions.\n\nA negative cost is unreachable here, where `cost(point)` answers Infinity. The two disagree on purpose: converting would mean a JavaScript pass over the array, which is precisely the loop this exists to avoid, and C3 has no infinity constant to write into a Float32Array instead. `costs[i] < 0` is the test; `Number.isFinite` is not."
			}
		},
		"Box3Helper": {
			"construct": "new three.Box3Helper(box, color = 0xffff00)",
			"note": "A wire box drawn exactly where a `three.Box3` says — the helper to reach for when the box came from somewhere that is not one object: a plot to fill, a gap to check, the union of two things.\n\n`box` is settable and the helper follows it. It is read in the frame of whatever the helper is added to, so a box from `boundsInParent()` belongs under the same parent and a box from `boundingBox()` belongs under the scene.\n\nDraws over everything: the line pipeline tests no depth, because the times you ask where something is are the times it is inside a wall.",
			"properties": [
				"box (settable — the helper moves and rescales to it)",
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"geometry",
				"children",
				"parent",
				"color (per copy, free: `[r,g,b]` or `0xff8800`)",
				"material (always null, and assigning throws — a helper draws with the line material)",
				"variant (meaningless here: the line material has no table)",
				"static (meaningless here: a helper casts no shadow to cache)",
				"animations (always empty)",
				"collides (already false in effect: a helper is in no query)",
				"morphs, weights (inherited from Mesh; a helper's geometry has no morph targets)"
			],
			"methods": [
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		},
		"BoxHelper": {
			"construct": "new three.BoxHelper(object, color = 0xffff00)",
			"note": "The box of an object and everything under it — \"how big is that actually, and where does it end\".\n\nIt must hang from the same parent as the object it measures, and is refused anywhere else: the box is measured in that frame, so a helper parented elsewhere would be drawn wherever the two frames differ, which is a box in the wrong place rather than no box.\n\nThe usual spelling is therefore the Three.js one — `scene.add(piece); scene.add(new three.BoxHelper(piece))` — and a nested piece takes `piece.parent.add(...)`. Nothing watches the object, so call `update()` after moving it.",
			"properties": [
				"object (what it measures, read-only)",
				"box (the box it is currently drawn on)",
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"geometry",
				"children",
				"parent",
				"color (per copy, free)",
				"material (always null, and assigning throws)",
				"variant (meaningless here)",
				"static (meaningless here: a helper casts no shadow to cache)",
				"animations (always empty)",
				"collides (already false in effect: a helper is in no query)",
				"morphs, weights (inherited from Mesh; a helper's geometry has no morph targets)"
			],
			"methods": [
				"update()",
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		},
		"AxesHelper": {
			"construct": "new three.AxesHelper(size = 1)",
			"note": "Red +X, green +Y, blue +Z from the origin — where a pivot is and which way it faces, which is the question a kit piece whose origin is in an unexpected corner makes somebody ask. Parent it to an object to see that object's pivot.\n\nIt is a Group of three meshes over one segment asset, so a hundred of them are still one draw call.\n\nRemember that a helper parented to a piece is inside that piece's box: align first, add the axes after.",
			"properties": [
				"size (settable — rescales the three arms, builds nothing)",
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"children",
				"parent",
				"static (meaningless here: a helper casts no shadow to cache)",
				"animations (always empty)",
				"collides (already false in effect: a helper is in no query)"
			],
			"methods": [
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		},
		"GridHelper": {
			"construct": "new three.GridHelper(size = 10, divisions = 10, color = 0x888888)",
			"note": "A ruled square in the XZ plane, centred on the origin: where the ground is and how big a metre looks.\n\nOne colour, not Three.js's two — the darker centre line would be a second mesh here for a distinction nothing has needed.\n\nKeyed on the divisions alone, so `GridHelper(100, 10)` and `GridHelper(40, 10)` are one asset at two scales and one draw call. There is no `size` to read back because the size is the scale: `grid.scale.x`, and it is live. Divisions are capped at 256.",
			"properties": [
				"divisions (read-only — a different count is a different mesh)",
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"geometry",
				"children",
				"parent",
				"color (per copy, free)",
				"material (always null, and assigning throws)",
				"variant (meaningless here)",
				"static (meaningless here: a helper casts no shadow to cache)",
				"animations (always empty)",
				"collides (already false in effect: a helper is in no query)",
				"morphs, weights (inherited from Mesh; a helper's geometry has no morph targets)"
			],
			"methods": [
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		},
		"WireframeHelper": {
			"construct": "new three.WireframeHelper(meshOrGeometry, color = 0xffffff)",
			"note": "A mesh's own triangles as the edges between them — the tool for two faces 0.01 apart z-fighting into a starburst, which is invisible in a solid render and obvious the moment the edges are drawn.\n\nTakes a Mesh that is already in the scene, or the geometry / `asset.mesh(name)` it draws.\n\nThe mesh has to be on the device. A generated shape is uploaded when it is constructed and works straight away, but a mesh out of a file reaches the device when something drawing it is added to a scene, and until then there are no triangles to read — you get a sentence saying so, not an empty helper.\n\nAdd it as a child of the mesh — `piece.add(new three.WireframeHelper(piece))` — because the edges are in the mesh's own space and a child at the identity transform overlays it to the pixel.\n\nEach shared edge is drawn once. A Group has no triangles of its own: traverse it and make one per Mesh.",
			"properties": [
				"of (the name of the mesh these edges belong to)",
				"position",
				"rotation",
				"scale",
				"visible",
				"name",
				"geometry",
				"children",
				"parent",
				"color (per copy, free)",
				"material (always null, and assigning throws)",
				"variant (meaningless here)",
				"static (meaningless here: a helper casts no shadow to cache)",
				"animations (always empty)",
				"collides (already false in effect: a helper is in no query)",
				"morphs, weights (inherited from Mesh; a helper's geometry has no morph targets)"
			],
			"methods": [
				"add(...)",
				"remove(...)",
				"traverse(fn)",
				"getObjectByName(name)",
				"getWorldPosition()",
				"boundingBox()",
				"boundsInParent()",
				"align(axis, edge, at)",
				"alignTo(other, opts)",
				"play(name, opts)",
				"stop()",
				"socket(bone)",
				"toJSON()"
			]
		}
	},
	"functions": {
		"three.load(path)": "Read a `.glb` or `.gltf`. Nothing is uploaded: this parses the JSON and answers with an Asset that knows its meshes, their bounds and the file's node tree. A mesh reaches the GPU when a Mesh drawing it is added to a scene, so loading a 200-piece kit to place twelve costs twelve.\n\nUnder `--assets` the path is relative to the assets directory and cannot climb out of it, so `three.inventory()` paths go straight in; otherwise it is relative to where three was started.\n\nLoading the same path twice returns the same asset — unless it was unloaded in between, which gives a fresh one and makes the old handle throw. Use `asset.instantiate()` for the file's own hierarchy and `asset.mesh(name)` for one piece of it.",
		"three.render(scene, camera)": "Draw one frame. `camera` is optional and must be `three.camera`.",
		"three.stats()": "The numbers in the stats section, for the whole scene, with culling off.\n\nThe six `...Ms` are the exception: they measure the last frame drawn rather than the scene, so they move when nothing about the scene has. `gpuMs` is the frame and the other five are what it was spent on — which is how you find out that a slow scene is slow in the shadow pass.\n\nThe four `...Bytes` that are not `textureBytes` or `poseBytes` — `geometryBytes`, `targetBytes`, `postBytes`, `shadowBytes` — are memory the renderer owns rather than the scene, so they read the same whichever scene you ask, and between them they are most of what a three process holds.",
		"three.scenes": "Every scene that exists, as `[{ id, active, held, nodes }]`. `stats().scenes` is the count; this is what the count is made of.\n\n`new three.Scene()` shows a new world without freeing the one before it, so a script that builds a scene per `run_script` holds every one of them — a node pool, a physics world and a set of asset references apiece, drawn by nothing.\n\n`held` says what `three.sceneById(id)` will give you back. True is the same Scene object with its children intact, and it stays true across `run_script`s. False means no Scene was ever made for it here — the scene the process starts with reads that way — and `sceneById` mints a handle instead.\n\n`three.disposeInactive()` frees them all without naming any.",
		"three.sceneById(id)": "The Scene for a host id — the one a script built, or a handle onto one nothing here wrapped.\n\n`three.sceneById(s.id) === s` for any scene this runtime built, including one built two `run_script`s ago, so identity survives the end of the call that made it.\n\nFor the other kind — the scene the process starts with — everything that goes through the host works: activate, dispose, stats, export, background, raycast, pick. But `children` is empty, because those nodes are the host's and no object was ever made for them. It is a handle, not the tree, and looking one up flips its `held` to true. `scene.id` is the other direction.",
		"three.disposeInactive()": "Free every scene except the one being rendered, then sweep. The level transition is: activate the next one, dispose the rest, `three.unloadUnused()` — and this is the last two.\n\nIt cannot leave the frame with nothing to draw, because the host refuses to dispose the scene being rendered, so activate what you are keeping first.\n\nAnswers with `{ scenes, assets, meshes, textures, bytes }`. The second half is often zero and that is right: two scenes over one kit means disposing either frees nothing, and `bytes` counts texture bytes alone. `stats().geometryBytes` before and after is the other half.",
		"three.unloadUnused()": "Free every asset no live mesh names, every mesh of a still-used file that nothing draws, and every texture that goes with them.\n\nAnswers with `{ assets, meshes, textures, bytes }`. `meshes` counts pieces given back without their file, which is what lets a level swap which parts of a kit it places without reloading the kit. `bytes` is texture bytes — the images that went, not the geometry — so a sweep that freed a hundred megabytes of vertex streams and no images answers 0; `stats().geometryBytes` is the other half.\n\n`scene.unload()` is this plus emptying the scene, and is what a level transition wants. An asset loaded but never added has no references either, so it goes too — load the next level after unloading, not before.",
		"three.quit()": "Close the window and end the process — what an in-game menu's Quit calls, and the only way a game closes itself.\n\nIt returns and the process is still up: the host closes between this frame and the next, because closing inside the click handler that asked would free the engine that handler is running in. So the frame already built is the last one shown and a fade-out ending on this call gets to finish — but nothing after the call is prevented from running either. Treat it as a request, not a return.\n\nNo argument and no exit code: a game that closed on purpose succeeded. Escape does the same in the window, but only under `--debug`, so this one works in every run and escape is not something to ship a menu on top of. A run with no host loop (a `--frames` batch) takes the request and never acts on it.",
		"three.reload()": "Boot this game again from its own source — the same thing shift+R does in the window under `--debug`.\n\nA new JavaScript context: the animation loop, every handler, every live object and every module the script imported are gone, and `main.js` runs again from the top.\n\nWhat survives is the machine rather than the world — loaded assets (`three.load` answers out of memory, no file is reopened), compiled pipelines (a material rebuilt from the same shader source is a cache hit, no Slang runs), the camera, and `three.persist`. Physics bodies and the nav bake go with their scene, which is freed. Materials and textures the old script held handles to are given back — nothing else would ever dispose them — so `stats().materials` and the images a `three.texture()` made drop to zero, while an image a resident asset owns stays with it.\n\nIt returns immediately and the reload has not happened yet: the host performs it between this frame and the next. Over `--mcp` that is the point — edit a `.js`, call this, and the next screenshot is of the edited file. A run with no host loop takes the request and never acts on it.",
		"three.persist": "The one object that survives `three.reload()`, and the whole of what does. Written by the game and read by the game, carried across as JSON, so what may go in it is what `JSON.stringify` accepts.\n\nPut numbers in it, not handles — a Mesh, a Scene or a body is an index into a pool that is about to be freed. A value it cannot serialise (a cycle, a Map, a function) is reported on the terminal and dropped rather than half-kept. Empty on a first boot.\n\nThe shape is `if (three.reloaded) player.position.set(...three.persist.at);` plus an animation loop that keeps `three.persist.at` up to date.",
		"three.reloaded": "False on the first boot of a process and true on every boot after a `three.reload()` or a shift+R under `--debug`. The only way a script can tell the two apart, and what decides whether `three.persist` means anything.",
		"three.inventory()": "Every `.glb` and `.gltf` under the assets directory, described without loading any of it: `[{ path, triangles, nodes, skins, meshes: [{ name, triangles }], animations: [name], bounds: { min, max } }]`.\n\nRead out of the JSON chunk, so it is cheap on a kit of any size — ask this before `three.load` to find out what is worth loading. `path` is what `three.load` wants. Empty when three was not started with `--assets`.",
		"scene.export(path)": "Write the scene to a `.glb`. Answers with `{ path, meshes, entries, materials, images, nodes, instances, batches, skipped, shaded, bakedImages, bakedColors, layers, bytes }`.\n\nOne mesh per unique (asset, mesh), so a thousand walls from one kit are one mesh in the file exactly as they are one draw call in the frame.\n\nSibling copies of one shape become a single node carrying an array of transforms — EXT_mesh_gpu_instancing, which any glTF reader can place — with a `_COLOR_0` array holding each copy's `mesh.color`, so a scene of many colours reloads as the one draw call it drew as. `batches` counts the nodes written that way.\n\n- A copy with no sibling drawing the same shape keeps its name and its own material, and groups are never collapsed.\n- Siblings sharing a shape but not a material do not batch: a colour travels per copy in `_COLOR_0`, but a texture, a blend mode and a layer stack have no per-copy channel.\n- Copies made with `asset.instantiate()` are not siblings — each arrives in its own group — so they do not batch. Pass `{ flatten: true }` to batch every copy of a shape in world space instead, giving up the hierarchy and the copies' names to do it.\n\nImages are written once and shared across every file they came from. Under `--assets` the path is inside the game directory and cannot climb out, as `three.load`'s is.\n\nHelpers and hidden subtrees are not in the file (`skipped` counts them), and a ShaderMaterial is not either, because it is a Slang pipeline and glTF describes surfaces rather than programs — those meshes export with the base colour and texture their geometry carries, and `shaded` counts them.\n\nPass `{ bake: true }` to get the shaders back. Each ShaderMaterial body is run over its own mesh's uv layout and read off the device, so what the frame showed reaches the file: a `baseColorTexture` where the answer varies across the surface, a `baseColorFactor` where it turns out to be one colour, and `alphaMode` MASK where the body discards, so leaves and netting come back as the shapes they cut. Without it, a scene whose character is thirteen shaders exports as a correct scene in a single grey — the tell is nine materials all carrying the same `baseColorFactor`.\n\n- `bakedImages` and `bakedColors` count the copies it managed; whatever is left is still in `shaded`, since a mesh with no uvs has no layout to rasterise into.\n- `true` bakes at 512 texels a side; a number picks another, 16 to 4096.\n- The cost is one render, one readback and one PNG per bake, and a body reading `s.position` needs one per node rather than one per shape. On a 2909-copy scene of world-space shaders that is 308 images: 14 MB at 256, 22 MB at 512, 44 MB at 1024, against 9.4 MB with no shading at all.\n- It is unlit on purpose — a viewer lights what it loads, and a baked-in sun would land twice — and it needs a GPU, which the rest of export does not.\n- A bake never splits a batch, so copies inside an instanced node share their batch's answer.\n\nMaterial layer stacks are the exception to that last part. A mesh loaded from a `.glb` carrying `CUSTOM_materials_layers` is written back with the stack it came in with; a `three.LayeredMaterial` built in a script is written from the description it was constructed with, images read back off the device. Either way the stack survives, and `layers` counts the records. The `COLOR_0` a VERTEX_COLOR mask reads goes into the file beside them. A script stack takes precedence when a mesh has both, because that is what the frame drew with, and changing a layer's map or animated tint after construction is picked up — the export reads live samplers and uniforms.\n\nThe scene around the meshes goes too: the camera as a glTF camera and every light as a KHR_lights_punctual directional light, each on its own node, so the file opens framed and lit the way three had it. The ambient floor has no glTF equivalent and is the one thing lost. Materials carry `side` as `doubleSided`, `repeat` and `offset` as KHR_texture_transform, and a source material's normal, occlusion and emissive maps with the metalness and roughness the specular term drew them with. Reflectance has no glTF slot that round trips, and lines are not written yet.",
		"three.renderSize()": "`{ width, height }` of the offscreen image — what `pick()` counts in and what a returned PNG is. `three.window` is how big the window showing it is, and the two are free to differ. `three.setRenderSize(width, height)` moves this one.",
		"three.setRenderSize(width, height)": "Pin the render size instead of letting it follow the window.\n\nBy default the offscreen target tracks the window's drawable — drag an edge, call `three.window.resize`, go fullscreen, and the picture moves with it. This is the escape hatch, and the reason for it is performance: a target below the window is upscaled to fill it, which is the render-scale slider a settings screen has. `three.setRenderSize(null)` gives the follow back.\n\nAfter it, `three.renderSize()`, the PNG a screenshot returns and the coordinates `scene.pick(x, y)` counts in are all the new size, and a post chain's images are rebuilt at it.\n\nReturns true when it has already happened and false when it is queued. Called from inside the animation loop it takes effect between that frame and the next, because the images it frees are the ones that frame is drawing into — the same deferral `three.reload()` and `three.quit()` use. Called from a booting script or an MCP tool call it is immediate, so the very next screenshot is the new size. Either way, read the size back on a later frame rather than on the next line.\n\nA size the device will not allocate throws, and the old target is still there and still being drawn. A run with no host loop never performs a queued one. Sizes are 1 to 16384 a side.",
		"three.configure(options)": "What a game declares about itself, at the top of `main.js`: `{ title, fullscreen, saveDir }`. Every key is optional and anything left out is left alone.\n\n```js\nthree.configure({ title: 'Wumpa Quest', fullscreen: false, saveDir: 'wumpa-quest' });\n```\n\nThere are no command-line flags for these — a player never sees a command line, and a settings screen has to change the same things at runtime — so `title` and `fullscreen` are live properties on `three.window` as well, and this is the one call that sets them before the first frame.\n\n`saveDir` is boot-only and has no property beside it, because moving it mid-run would strand everything already written. It is a folder name and not a path; a separator in it is refused.\n\nReturns `{ title, fullscreen, saveDir }` as they stand after the call, so a boot log can print one accurate line. Under `--headless` the window half is remembered and does nothing.",
		"three.save": "Read and write the game's own save folder — the one place a script may write outside its assets directory.\n\n- `write(name, value)` / `read(name)` — JSON.\n- `writeText(name, string)` / `readText(name)` — a string.\n- `writeBytes(name, bytes)` / `readBytes(name)` — a Uint8Array.\n- `list()` — every save in the folder. `remove(name)` deletes one, answering false for a slot that was not there.\n- `path` — where the folder actually is, or null when nothing has named one.\n\nThe folder is `<application data>/three.c3/<name>`, named by `three.configure({ saveDir })` or taken from the assets directory's own name: `~/Library/Application Support` on macOS, `%AppData%` on Windows, `$XDG_CONFIG_HOME` or `~/.config` elsewhere. Deliberately not the cache directory the compiled shaders go in — a disk cleaner may empty that one.\n\nA save is a name, not a path: letters, digits, dash, underscore and dot, at most 64, not starting with a dot. There are no subdirectories and no way to leave the folder, so `'a/b'` and `'../escape'` throw rather than resolving.\n\nReading something never written answers null rather than throwing, because that is every game's first run, and `list()` is `[]` before a folder has even been named, so a load menu needs no guard. A `--script` or `--mcp` run has no assets directory to be named after: saving there throws until `three.configure` names a folder.",
		"three.window": "The window: `width`, `height`, `scale`, `resize(width, height)`, `title` and `fullscreen`.\n\n`width` and `height` are device pixels, read off the drawable so they stay current through a live resize drag; `scale` is device pixels per logical point — 1.0 on an ordinary display, 2.0 on a retina one. `resize` takes those same device pixels, so asking for a size and reading it back agrees, and it answers with whether there was a window to ask.\n\n`title` is writable at any time — `three.configure` names it at boot and this is what a level change or a pause menu uses — and reads back what was last set even under `--headless`.\n\n`fullscreen` is a request in both directions: macOS animates into its own space over about half a second, a Wayland compositor answers with a configure some frames later, and an X11 window manager may refuse outright. Read it back on a later frame.\n\nThe picture follows the window: the offscreen target moves to the new drawable, so resizing or going fullscreen renders more pixels rather than stretching the ones there were, and `three.renderSize()`, the PNG a screenshot returns and the coordinates `scene.pick(x, y)` counts in all move with it. `three.setRenderSize` pins it when a game wants a render scale of its own, and resizing past a pinned size says so in the run's warnings.\n\nResize is a request, not a setting: the new size turns up on a later frame rather than on the next line, so read `three.window.width` back from inside the animation callback.\n\nOn Wayland the size never reads back — the surface answers \"whatever the swapchain asks for\" — so the drawable stays what the process booted at and the compositor scales it. X11, macOS and Windows track it. Under `--headless` sizes are zero, `fullscreen` is false and `resize` returns false.",
		"three.getApiDocs(options)": "This documentation, four ways.\n\n- No argument — the index: the summary, every difference from Three.js, the stats block, the key names, the examples, and the names of the classes and functions. About a quarter of the whole, and the part read every time.\n- `{ search: \"shadow\" }` — the grep: every entry whose name or prose mentions a word, in full, keyed by the path that asks for it again.\n- `{ section: \"classes.ShaderMaterial\" }` — one entry or one whole section. A bare `\"ShaderMaterial\"` is found too.\n- `{ all: true }` — everything at once.\n\nOver MCP these are `get_api_docs`'s arguments, plus a `path` that writes the whole surface to a Markdown file — one heading per entry — which is how you grep it with your own tools.",
		"three.searchDocs(term)": "Where does the API mention X, over the whole documentation rather than only the differences.\n\nAnswers `{ query, matches, entries }`, where `entries` maps a path like `classes.ConvexGeometry` or `functions.three.load(path)` to the text at it. One answer is capped, and anything that did not fit is named in `notShown` rather than dropped silently.\n\n`three.searchDocs(\"keyboard\")` finds the headless-has-no-keyboard note without dumping the whole documentation object. Same thing as `three.getApiDocs({ search: term })`.",
		"three.budget": "How long this script may run before the interrupt stops it, in milliseconds. 30,000 by default through `run_script` and 5,000 everywhere else — a tool call is already asynchronous to whoever made it, where a window is somebody watching a frame.\n\nRaise it to simulate, not to build: five seconds is generous for assembling a scene and short for stepping one. Raising it applies to the run that raises it, because a script does not know it needs longer until it is already running.\n\nTen minutes is the ceiling and asking for more clamps rather than throws; zero or negative throws, because there is no way to turn the interrupt off. It does not reach the animation callback, which keeps its own 100 ms so that one slow frame is a stutter rather than a hang.",
		"three.budget (a COLD scene build over the budget)": "A `run_script` that rebuilds a scene from scratch can still run past 30,000 ms, and the error names the fix.\n\nOn a fresh `--mcp` backend the first build pays for shader compiles and texture uploads that a warm (`--script`-preloaded) backend already did, so it can cross the budget even when the same script returns in time on a second call.\n\nThe fix is one line at the top of the script, before any build: `three.budget = 60000;`. Better still, preload the scene so the build runs outside the budget — launch the backend with `--script scenes/mine.js` and let `run_script` move the character rather than rebuild it. Reading `three.budget` back answers the current limit.",
		"toJSON() / toString()": "What `JSON.stringify` sees, and therefore what comes back in the `value` field when you return an object from a script. Objects report their name, transform and children; a Vector3 reports `[x, y, z]`; a ShaderMaterial reports its fragment and uniforms.",
		"three.query.sphere(centre, radius, into)": "Every drawable node whose bounding box reaches within `radius` of a point.\n\nWith no third argument it answers with an Array of objects; with a `three.query.buffer(n)` it fills that and answers with a count, which is the form to use in a loop.\n\nA broad-phase answer: the test is against each node's box, so something whose box reaches and whose triangles do not is included. Invisible nodes are left out, as they are for picking.",
		"three.query.box(box, into)": "The same, for a Box3, a `{ min, max }` or a flat `[minX, minY, minZ, maxX, maxY, maxZ]`.",
		"three.query.buffer(capacity)": "A reusable answer for the flat form of every query verb. Make it once, outside the loop.\n\nIt holds `.handles` (an Int32Array of index/generation pairs), `.count`, `.full` — true when the query filled it, which means there may be more — and `.objects()`, which resolves them all in one walk of the scene. A script that only wants to count what is nearby never has to resolve anything, which is most of why this shape exists.",
		"three.query.raycastAll(origin, direction, options)": "Every node a ray hits, not only the nearest — shooting through a window, listing what is behind what, a laser that stops at the first solid thing rather than at the first thing. Options are `{ maxDistance, limit }`.\n\nNot sorted by distance: sorting means holding every hit before answering, and a caller who wants the nearest should call `scene.raycast`, which is cheaper than sorting because it can stop walking.",
		"three.query.sweep(from, to, options)": "Move a sphere or an upright capsule from one point to another and report the first thing it touches, or null.\n\n`{ radius }` alone is a sphere; adding `{ height }` makes it a capsule that tall overall; `{ ignore }` leaves objects out, taking an object or an array of them and leaving each one's whole subtree out.\n\nThe hit carries `fraction` — where along the motion it happened — so the safe position is `from + (to - from) * fraction`.\n\nThis is the same narrow phase `three.moveAndSlide` is built out of, so a wall this reports and a wall a character slides along cannot be two different walls. There is no orientation argument: everything this is for stands up.",
		"three.moveAndSlide(position, motion, options)": "The character controller. Sweeps a capsule from `position` by `motion`, slides along whatever it hits, climbs a ledge under the step height, and answers with `{ position, remaining, grounded, slope, ground, hit, normal, stepped, slides }`.\n\nOptions are `{ radius, height, step, slope, skin, snap, ignore }`:\n\n- `height` is the whole capsule.\n- `step` is how high a ledge may be.\n- `slope` in degrees is the steepest ground that still counts as ground.\n- `snap` is how far the floor may drop under the feet in one frame and still be walked down rather than fallen off.\n\nIt integrates nothing: gravity, the velocity and the jump are yours. A frame is `three.moveAndSlide(where, [vx * dt, vy * dt, vz * dt], opts)` followed by copying `r.position` onto whatever you are driving. It is kinematic and touches no rigidbody.\n\nPass the character's own object as `ignore`, and pass the Group rather than one mesh out of it: `ignore` leaves the object's whole subtree out, so a character built from a body, a head and four limbs does not collide with its own chest. It takes an array too, for up to eight things — a ninth throws rather than being silently dropped. It is per call; a thing that should never be collision geometry for anybody is `object.collides = false`.",
		"three.batch(objects, options)": "A Float32Array-shaped bulk write over many nodes. `batch.positions` is three floats per object, seeded from where they are now; `batch.flush()` sends the lot in one crossing and answers with how many landed. With `{ trs: true }` the stride is ten — position, an xyzw quaternion, then scale.\n\nA member that has left the scene is skipped rather than throwing, because a crowd where one agent was removed this frame is ordinary.\n\nDo not reach for this to move a dozen things: five hundred ordinary position writes are three per cent of a frame, and the trigger for this is about two thousand nodes a frame.",
		"three.moveAndSlideAll(positions, motions, options) / three.moveBuffer(n) / three.moveResult": "`three.moveAndSlide` for a whole crowd, in one call — the same controller and the same sweeps. It exists for the shape of the answer rather than for the crossing: the single form measures 8.32 µs per agent and this one 1.43 µs, so two hundred agents cost 0.29 ms of a fixed step instead of 1.66 ms. What went away is a JavaScript result object per agent.\n\n- `positions` is the capsule centre, three floats per agent, read and written in place — it is your position column, so there is nothing to copy back.\n- `motions` is the whole step's motion, three floats per agent, read.\n\nOptions are `{ radius, height, step, slope, skin, snap, ignore, self, results }` — the same six numbers the single form takes, one set for the whole crowd, because a crowd is one agent size.\n\n- `self` is how an agent stops colliding with its own mesh, and it is a column rather than one object: two ints per agent, that agent's node and generation, whose whole subtree it passes through. `three.batch(objects).handles` is already exactly that array; an array of objects also works.\n- `ignore` is still the shared set — the lift everybody rides — and takes at most seven alongside a `self` column, because the eighth slot is the agent itself.\n- `results` is optional and is a `three.moveBuffer(n)`: eight floats per agent, laid out by `three.moveResult` — `remaining` at 0, `normal` at 3, `slope` at 6, and a flags float at 7 holding `moveResult.GROUNDED | STEPPED | TOUCHED`. Leave it out and only the positions are written.\n\nEveryone moves at once: every agent is swept against the world as it was when the call started, so agent 3 does not see agent 2's new position. Resolving in array order instead would make the answer depend on how you happened to store your crowd, and `three.steer`'s separation already assumes simultaneity. Two agents can therefore end a step overlapping; separation keeps that rare and the next step's depenetration resolves it.\n\nIt answers with no node handles. The single form reports `ground` and `hit`; a flat float array cannot, and \"what am I standing on\" is a moving-platform question belonging to the one character riding the platform — that character calls `three.moveAndSlide`.",
		"scene.pick(x, y)": "What is under a pixel of the rendered image, counted from its top-left corner. Answers with an intersection or null. Needs a GPU device.",
		"scene.raycast(origin, direction)": "What a world-space ray hits. Either vector may be a Vector3, an `{x, y, z}` or an `[x, y, z]`, and the direction need not be normalised. Answers with an intersection or null.",
		"three.nav.bake(options)": "Voxelize the scene's standing room, so `three.nav.path` and `three.nav.field` have a graph to work over. `{ cell, radius, height, slope, bounds }` — every one a property of the agent except the last.\n\nCall it after the level is built. Nothing bakes on demand, because that would hide a cost and rebake on the first call after anything moved.\n\n`cell` decides everything: it is the resolution and also the largest step that can be climbed, because two cells are connected when they are adjacent and one cell up. Half a metre is a generous stair and a cheap bake; a finer one costs as the cube.\n\nAnswers with the same object `stats()` does, or null when the region held no standing room, which is an answer and not an error. A second bake replaces the first.\n\nThe bake belongs to its scene — `scene.nav` is the one that does, `three.nav` is whichever scene is being rendered — so it survives a switch, and the next level can be baked before it is shown.\n\nCheck `components` on what comes back: anything above 1 is a level cut into islands, and every other number will look healthy while half your agents stand still.",
		"three.nav.stats()": "What the last bake produced and what it cost: `{ cell, radius, height, slope, voxels, solid, floor, walkable, components, largest, bakeMs, bounds }`, or null if there has not been one.\n\n`bakeMs` and `voxels` decide whether baking is a level-boundary operation or a loading screen for your level rather than for a reference one.\n\n`components` is the one to check, and it is not a cost: it is how many disjoint regions the standing room came out in, with `largest` the size of the biggest. Every other number here is a total, and a total cannot tell a level an agent can cross from the same level in pieces — a doorway one cell too narrow, a step one cell too high or a ramp that does not quite reach all leave `walkable` looking right, `field()` returning a live field rather than null, and `direction()` answering `(0, 0, 0)` for every agent on the wrong side of the break. Above 1 means \"there is no path\" is the honest answer for some pairs of points, and the usual cause is a `cell` too coarse for the geometry.",
		"three.nav.path(from, to, options)": "One agent, one route: an Array of Vector3 waypoints on the walking surface, starting at the point given, or empty when there is no route. `{ limit }` caps the waypoint count.\n\nThe straight lines between them have been checked with a capsule sweep at the agent's own size, so a corner it cannot physically round has not been cut and the path does not look like it is walking cell centres.\n\nThis solves a whole field and throws it away — right for a wanderer replanning every few seconds, and the thing `three.nav.field` exists to replace for a crowd.",
		"three.nav.field(goals)": "Solve towards one or more goals and keep the answer — the verb a crowd uses.\n\nTakes a point or an array of points. Many goals is not several routes: it is one field whose value is the distance to the nearest of them, which is what a crowd heading for whichever exit is closest wants.\n\nReturns a NavField with `direction(point)` — a unit XZ vector, or zero for nowhere to go — `cost(point)`, which is Infinity for unreachable, `reaches(point)` and `dispose()`.\n\nReturns null when no goal landed on a walkable cell, which is the usual way a goal is wrong: given at the height of the floor's underside, inside a wall, or outside the baked region.\n\nFeed the field to `three.steer` rather than calling `direction()` in a loop.",
		"three.steer(positions, velocities, options)": "Seek, arrive and separation over a whole crowd in one crossing. `positions` and `velocities` are Float32Arrays of three floats per agent — the first read, the second written.\n\nOptions are `{ field, goal, maxSpeed, arrive, separation, separationWeight }`. A field wins over a goal, because a field already knows the way round a wall. `arrive` is how far out an agent starts slowing, and 0 never slows — which is how a crowd ends up orbiting a door.\n\nWhat comes back is a desired velocity: integrate it yourself, and feed it to `three.moveAndSlide` for agents that collide or add it straight on for agents that do not.",
		"three.catmullRom(points, options)": "A smooth curve through sparse control points, as a dense polyline — the path half of the curve pair (RibbonGeometry is the mesh half).\n\nPass `[[x, z], ...]` or `{x, z}` control points and get back `[[x, z], ...]` with `samples` points per control segment (default 16). Type is `'centripetal'` (default), `'chordal'` or `'uniform'`; centripetal passes through every point without swinging wide of a tight one, which is what a hand-written road path is. The first and last control points are reproduced exactly.\n\nFeed the result to `field.carve` / `field.stroke` / a scatter avoid corridor so a sparse polyline stops being a black-and-white zigzag, or pass the raw control points to a RibbonGeometry, which curves them itself.",
		"three.clamp(v, min, max) / clamp01(v) / lerp(x, y, t) / inverseLerp(x, y, v) / mapLinear(x, a1, a2, b1, b2)": "The scalar block, with Three.js's MathUtils names and Three.js's argument order.\n\n`clamp01` is GLSL's saturate and has no Three.js equivalent. `inverseLerp` answers 0 when `x` and `y` are equal, because the honest alternative is a division by zero that reads as the whole gradient disappearing.\n\nThese stay in JavaScript on purpose: a host call that allocates in order to answer arithmetic measures 185 ns against the 70 ns of the JavaScript it replaced.",
		"three.smoothstep(x, min, max) / smootherstep(x, min, max) / band(x, lo, hi)": "The 0..1 ramp with flat ends — and the argument order is Three.js's, not GLSL's. GLSL is `smoothstep(edge0, edge1, x)`; this is `smoothstep(x, min, max)`, value first. Every shader body in this project uses the GLSL order and every script uses this one, so the two sit a few lines apart in the same file and swapping them is silent: the answer is still a number in 0..1, just the wrong one.\n\n`smootherstep` has a zero second derivative at both ends (Perlin's), and is what to use when a smoothstep ramp still shows a crease where it meets the flat part.\n\n`band` is a smooth bump — 0 outside `lo..hi`, 1 in the middle — with no Three.js equivalent. It is the splat-mask verb, and the reason a layered terrain reads as bands of material rather than one gradient.",
		"three.pingpong(x, length) / euclideanModulo(n, m) / degToRad(d) / radToDeg(r)": "All Three.js MathUtils names.\n\n`euclideanModulo` answers with the sign of the divisor, so `-1 % 4` is 3 rather than -1 — which is what a wrap-around index or a tiling coordinate wants, because JavaScript's `%` is a remainder and the negative half of every tiled texture is where that shows. `pingpong` ramps up to `length` and back down forever.",
		"three.moveTowards(current, target, maxDelta) / moveTowardsAngle(current, target, maxDelta)": "Step towards a target at a fixed rate, stopping exactly on it — the linear sibling of `three.damp`.\n\n`damp` closes a fraction of the gap per second, so it is fastest at the start and never quite arrives, which is right for a camera easing onto a target. This closes `maxDelta` per call and lands exactly, which is what a turn-rate limit, a reload timer, a fuel gauge and an ammo counter want: anything whose speed is a rule rather than a feel.\n\n`maxDelta` is a distance, so it is `rate * dt`. `moveTowardsAngle` is the same, taking the short way round a circle.",
		"three.wrapAngle(radians) / angleDelta(from, to)": "The seam at ±pi, named.\n\n`wrapAngle` folds an angle into `[-pi, pi)` — half open at the top, so exactly pi comes back as -pi, which is the same heading.\n\n`angleDelta(from, to)` is the short way between two headings, signed. A heading of +3.1 against a target of -3.1 is 0.08 radians apart this way and 6.2 the straight way, and a character told to turn 6.2 radians spins a full circle to arrive somewhere it was already almost pointing.\n\n`three.dampAngle` and `three.moveTowardsAngle` are both written in terms of `angleDelta`, so there is one spelling of the wrap rather than three.",
		"three.mixColor(a, b, t) / tintColor(colour, k)": "Colour arithmetic over whatever `mesh.color` takes — a hex like `0xff8800`, an `[r, g, b]`, an `[r, g, b, a]` or an `{r, g, b}` — answering with four components, so the result feeds straight back into `mesh.color`, a uniform, or another one of these.\n\n`t` is not clamped, for the same reason `lerp` does not clamp. `tintColor` scales brightness and leaves alpha alone, because a tint that also faded the thing out would be a surprise.",
		"three.seed(n) / randFloat(low, high) / randInt(low, high) / randFloatSpread(range)": "Randomness that can be replayed. These keep Three.js's names and do not call `Math.random`: they draw from a seeded stream `three.seed(n)` resets.\n\nThat is a deliberate divergence. `Math.random` throws away the determinism the rest of the engine goes to some trouble to have — the fixed step, the solver's own accumulator and `state_hash` all exist so that the same inputs produce the same frame, and one `Math.random()` in the gameplay layer costs all of it: a bug that reproduces on the tester's machine and not on yours, with no way to bisect. A script that wants an unrepeatable number still has `Math.random`.\n\n`randInt` is inclusive at both ends, as Three.js's is. `new three.Random(seed)` is the same generator owned by the caller, for when two systems must not perturb each other's sequence.",
		"three.hash(x, y, seed) / noise2(x, y, options) / fbm2(x, y, options)": "Noise, sampled at a point rather than baked into a grid — which is the shape that composes: the same call fills a texture in a double loop, feeds `field.fill((x, z) => ...)` for terrain, and answers a single spawn test.\n\n- `hash` is three ints in and a number in 0..1 out, and the same three always answer the same.\n- `noise2` is smooth value noise on a unit lattice, one feature per unit of x and y, taking `{ seed, period }`.\n- `fbm2` is `octaves` layers of it, each twice as fine and half as strong, normalized back to 0..1 — the verb behind every generated rock face, bark, dirt and cloud in `examples/` — taking `{ octaves, seed, period, lacunarity, gain }`.\n\n`period` is what makes it tile: pass the number of cells across the image and the left edge meets the right. `fbm2` tiles correctly only because each octave's period is scaled with its frequency, which is the part that is wrong when it is written out by hand — and the reason a hand-rolled tiling fbm shows a seam at exactly one octave's worth of the image.",
		"three.damp(current, target, lambda, dt)": "Move `current` towards `target` by a fixed fraction of the remaining distance per second.\n\n`lambda` is the rate — 1 is lazy, 5 is a normal follow, 20 is nearly rigid — and `dt` is in seconds: `three.clock.dt` as it comes. Three.js spells this `MathUtils.damp`.\n\n`three.dampAngle` is the same taking the short way round a circle, which is what a heading needs and what the plain one gets wrong at exactly the place a mouse look crosses constantly.",
		"three.smoothDamp(current, target, state, smoothTime, dt, maxSpeed)": "A critically damped spring — the one to reach for when `damp` overshoots the feel you wanted. The difference is momentum: `damp` is fastest at the start and asymptotically slow at the end, right for a camera easing onto a target and wrong for anything that should look accelerated.\n\n`state` is an object this writes `.velocity` into, and it must outlive the frame. One created inside the loop is a spring re-launched from rest sixty times a second, which looks exactly like `damp` with a worse constant and is the one way to use this and see nothing.\n\n`smoothTime` is roughly how long the move takes, in seconds.",
		"new three.CatmullRomCurve3(points, closed, curveType, tension)": "The three-dimensional half of the curve pair, and the one a loop samples rather than a bake consumes: a camera rail, a patrol route, a rope. Three.js's class and method names — `getPoint(t)`, `getPointAt(u)`, `getTangent(t)`, `getLength()`, `getPoints(n)`, `getSpacedPoints(n)`.\n\n`getPoint` and `getPointAt` are not the same function, and the gap between them is what makes hand-written rail code look wrong. `t` is the curve's own parameter, spread evenly over the control segments, so an object moving at a constant `t` per second speeds up through the widely spaced ones and crawls through the close ones. `u` is spread evenly over the length, and that is the one anything moving wants.\n\n`three.catmullRom` is the other half: a ground path, `[x, z]` in and a dense polyline out, for `field.carve`, `field.stroke` and RibbonGeometry.",
		"three.scatter(options)": "Where to put a hundred trees: `{ count, seed, onTerrain, bounds, spacing, minHeight, maxHeight, maxSlope, avoid, accept }`.\n\nReturns `[{ x, y, z, normal, index }]` — placements, not meshes, because the loop that turns a placement into a mesh is three lines and the caller almost always wants to vary the colour or the scale per point.\n\n- `onTerrain` is a TerrainGeometry or a Field and supplies the height and the normal; `bounds` defaults to that terrain's own extent.\n- `avoid` takes `{ x, z, radius }` circles and `{ path, width }` corridors, so the same polyline that carved the river keeps the trees out of it.\n- `maxSlope` is in degrees off flat.\n- `spacing` is a minimum separation enforced by rejection, not a guarantee: the sampler gives up after a bounded number of tries and returns a shorter list rather than spinning, so read `.length`.\n\nThe same seed places the same points, which is what makes a screenshot comparable to yesterday's.",
		"three.systems.step(name, fn, options) / frame / add / remove / enable / list / outline / report / clear / three.systemLoad(budgetMs)": "The ordered system registry. `three.setFixedLoop` and `three.setAnimationLoop` each take one callback, so a game with five things to do a frame has one function with five things in it — and that is what this replaces.\n\nIt makes nothing faster and is not meant to: every JavaScript-side data layout measured inside the noise floor of the measurement itself. What it makes is a frame you can read and a slow one you can attribute.\n\nThe verb is the clock:\n\n- `three.systems.step(name, fn)` is the fixed one — zero or more times a frame at `three.clock.fixedRate`, the same `dt` every call — and it is where the rules of the game go, because movement and collision drift when the step they integrate over does not.\n- `three.systems.frame(name, fn)` is once per drawn frame, handed what that frame was actually worth, and it is where the camera, the fades and the uniform writes go.\n- `add()` is the same thing with `{ phase }` left as an option, for when the phase is a variable.\n\nThey run in the order you register them. For the one system in a file that has to break that, name the neighbour: `{ before: 'fire' }` or `{ after: 'Player.pose' }`, one name or a list, and a name registered further down the file is fine. `{ first: true }` and `{ last: true }` are the two ends of the game's own list — the engine's systems still bracket them, the rules drain ahead and the entity write-back behind. A loop is refused where it is made; a name that matches nothing is said once and otherwise ignored. There is also a numeric `{ order }`, which is what the engine's own systems use, but a game should not need it.\n\n`outline()` prints the tick — `step  spawn → player → foes` on one line and `frame autopilot → fire` on the next. Two lines because they are two lists: every step this frame owes runs before any frame system.\n\nAdding a name that exists replaces it, so a re-run top level ends up with one copy of each system rather than two.\n\nSystems are handed seconds, unlike the animation callback, which keeps Three.js's milliseconds because it keeps Three.js's name. `three.setAnimationLoop` and `three.setFixedLoop` are themselves systems now, under the reserved names `animation` and `fixed`, which is why a script that has never heard of this is unaffected and why a later `setAnimationLoop` cannot silently evict the whole list.\n\nA system that throws does not stop the others — that is most of the point, because with one callback a throw in the fruit code stops the camera. It is not swallowed either: the message names the system, repeats a few times and then goes quiet, and `report()` keeps counting. Nothing is disabled behind your back.\n\n- `list()` answers `[{ name, phase, order, enabled }]` in the order they run, steps first.\n- `report()` answers `[{ name, phase, enabled, ms, peak, calls, errors }]` most expensive first, where `ms` is a rolling average of milliseconds per frame — so a fixed system that ran four times reports what all four cost — and `peak` is the worst frame since it started, because a mean of 0.4 ms hides a system that spends 9 ms once a second and that is the one a player feels.\n- `three.systems.frameMs` is the total and `three.systemLoad(budgetMs)` is it as a 0..1 fraction.\n\nTiming costs two `three.clock.wall` calls per system per call, about 3 µs a frame for ten systems; `three.systems.profile = false` turns it off. `clear()` forgets everything, and `new three.Scene()` deliberately does not — a Scene is the contents of the world and these are the rules it runs under.",
		"three.cooldown(duration, options)": "A scalar timer for the `if (x > 0) x -= dt` pattern — a spin window, a hurt window, coyote time.\n\nIt is ticked by a lazily-registered `three.systems` entry rather than read off `three.clock.time`, because the game clock advances once per host tick and a window shorter than one fixed step would see no time pass across a multi-step catch-up frame. A paused clock still freezes it either way.\n\nSee the Cooldown class for `start`, `cancel`, `active`, `ready`, `recovering`, `remaining`, `progress` and `elapsed`.",
		"three.Entity": "The base class, and the shortest way to an entity: `class Critter extends three.Entity`, with no registration call beside it.\n\nThe class registers itself on first use — a spawn, a rule, an `of()` — reading its own statics: `capacity`, `columns`, `parent`, `body`, `volume`, `trigger`, `collides`, and `name` (the class name unless a static `name` overrides it).\n\n`super()` first in the constructor, and it is not a formality: that is where a bare `new Critter()` is refused, which is the one check a class with no columns cannot get any other way. An untracked instance has no node in the scene, no body in the solver and no place in the live list, and the first sign of it is a thing that never appears.\n\nEverything `three.track` installs is inherited: `spawn`, `of`, `remove`, `column`, `all`, `count`, `free`, `compact`, `clear`, `dispose`, `on`, `off`, `system`, `pose`, `flush`, `sync`, `handles`, `transform`, and iteration.\n\nThere is no `update()` to override, on purpose: continuous work is a system with a readable `{ order }`, and a per-entity update method is the ninety-line animation callback again, once per class.",
		"three.track(Class, options)": "The same registration as `three.Entity`, for a class that already has a parent and cannot extend it — the options are the argument here and the statics there, and nothing else differs.\n\nIt answers with a Proxy that refuses a bare `new Class()`; capturing it is optional, because every static is installed on the class itself.\n\nIt owns the object → instance map, the spawn ritual, the body, the volume, the live list and a compaction registered as `<Class>.compact` — so `Class.of(hit.object)` is the instance and `c.remove()` is the whole removal.\n\nOptions are `{ parent, columns, capacity, body, volume, trigger, collides, name }`:\n\n- `columns` is `{ position: 3, motion: 3 }` and needs a `capacity`, because a column cannot grow without dangling every live window into it.\n- `body` is what `three.physics.add` takes, or a function of the instance answering one.\n- `volume` is `{ shape: 'capsule' | 'sphere' | 'box', radius, height | size, offset }` for an invisible kinematic body beside the drawn node, carried by `<Class>.follow`; `trigger` is the same with `trigger: true`.\n- `collides` is written to every mesh in `this.object`'s subtree.\n- `parent` hangs the nodes off a Group. It is refused alongside a `body` — a body-backed node has to be a direct child of the scene — but allowed alongside a `volume`, which is its own node and reads a world position to follow.\n\nThe constructor sets `this.object` to the Mesh or Group, and may write `this.position[0]` on line one: the column slot exists before it runs.\n\n`Class.spawn(...args)` is the only way in. `new Class()` throws, because an untracked instance has no slot, no body and no place in the live list and every one of those failures is silent.\n\nStatics: `spawn`, `of`, `remove`, `column(field)`, `all()`, `count`, `free`, `capacity`, `compact()`, `clear()`, `dispose()`, `on()`, `off()`, and iteration — `for (const c of Critter)`. Instance hooks: `onSpawn()` and `onRemove()`. There is no `update()` to override, for the reason given under `three.Entity`.",
		"three.instanceOf(object)": "Which tracked instance owns this object — a drawn node, one of its child meshes, or a volume — or null.\n\nIt walks up the parent chain, because a raycast and a query answer with the leaf that was hit and an assembled character is a Group of eleven meshes. The global reverse of `Class.of(object)`, and what a rule keyed on \"what are these two things\" opens with.",
		"three.emit(a, verb, b)": "The game raising its own event: `three.emit(player, 'use', door)` reaches `Door.on('use', Player, fn)` exactly as a trigger reaches an `'enter'` rule, so a rule cannot tell whether a solver or a keypress raised it. Either argument may be an instance or an Object3D.\n\nIt dispatches at once, where an engine event is queued and delivered by the `rules` system — a handler that deletes a body from inside the solver is a hazard the game cannot see, and the game knows when it is safe where the solver does not.\n\nAnswers with how many rules fired.",
		"three.rules()": "Every registered rule: `{ name, event, subject, matcher, order, enabled, failures }`.\n\nThe pair-dispatch half of `three.systems.report()`, and how you find the rule that is throwing sixty times a second after its message has stopped repeating in the log.",
		"three.setAnimationLoop(fn)": "Run `fn(elapsedMs)` once per frame, or null to stop. Synchronous only. The next `run_script` reports how many frames it ran, whether it is still running, and why it stopped if it did.\n\nOnly one callback exists: registering a second replaces the first.\n\nIt survives `new three.Scene()`, and so does the scene it was built for, so a callback holding meshes from an older scene goes on moving them where nobody can see them — stop it yourself, or re-register it after the swap. It only throws once that scene is disposed.\n\nThe milliseconds are the game clock (`three.clock.time * 1000`), so they stop when `three.clock.timeScale` is 0 and start at 0 on the first frame rather than carrying the boot.",
		"three.setFixedLoop(fn)": "Run `fn(dt)` at a fixed rate — zero or more times per frame, as many as the clock owes at `three.clock.fixedRate` (60 Hz by default), capped at eight.\n\n`dt` is the same number every call, in seconds, which is what makes gameplay written against it produce the same result on a slow machine as on a fast one. This is where movement, timers and rules belong; `setAnimationLoop` is where drawing the consequence belongs.\n\nThe accumulator is the host's, not yours: one written in the animation callback spends the script budget catching up and gets the callback stopped instead of stuttering.\n\nRuns after the frame's physics and before the animation callback. Same rules as `setAnimationLoop` — synchronous, one of them, null stops it.",
		"three.frame": "How the frames have gone, and where the last one went: `{ running, ticks, overruns, ms }`.\n\n`overruns` is how many frames spent more than 8 ms in JavaScript — half a 60 Hz frame, the point at which the script has stopped leaving room for the draw it sets up. Nothing is logged when it happens, so this is the number to read if you suspect hitching. Under `--mcp` alone it stays 0, because there an overrun stops the callback instead of counting it.\n\n`ms` is the last finished frame, split five ways — read it from inside a system and it describes the frame before, which is complete. `{ handlers, fixed, frame, jobs }` are the four spans that add up to `total` and are what the 8 ms is measured against:\n\n- `handlers` — key, click and physics-trigger handlers.\n- `fixed` — every fixed step this frame owed, together.\n- `frame` — the animation callback and the frame-phase systems.\n- `jobs` — one queued mesh upload and the microtasks it settled.\n\n`solver` is outside `total`, because it is outside the budget: the physics step runs above the script's window and is the host's own work, so a callback is never stopped or counted for it. It is reported because a frame that spends 10 ms in the solver and 3 ms in script is a frame whose script is not the problem.\n\n`three.systems.report()` is the rolling per-system version and the one to reach for next: this splits a frame into four spans, that splits two of those spans by name.",
		"three.clock.wall": "The process's own monotonic clock, in milliseconds, and the one reading on `three.clock` that is not game time.\n\nEverything else there is scaled by `timeScale` and stops dead when paused, which is what makes `x += speed * three.clock.dt` need no check — and what makes it useless for the one question a profiler asks, because a system timed on the game clock reads zero while paused and four times its true cost in slow motion.\n\nSo this answers a different question: how long did that take, in real milliseconds, whatever the game clock is doing. Two readings and a subtraction; a host call answering a number is 143 ns. `three.systems.report()` is built on it and is usually what to reach for instead.\n\nIts origin is when the JavaScript runtime opened and is shared with nothing else — differences are what it is for. Read only.",
		"three.clock.time / three.clock.dt": "The game clock, in seconds. `time` is what the frames have added up to and `dt` is what the frame being drawn is worth — 0 before the first frame and 0 while paused, so `x += speed * three.clock.dt` needs no check for a pause.\n\n`dt` is clamped: a frame that took longer than 100 ms of wall time reports 100 ms, so a breakpoint or a long tool call stutters rather than teleporting the world a second forward. Both are read-only.",
		"three.clock.timeScale / three.clock.paused": "Wall time to game time: 1 is real time, 0.25 is slow motion, 3 is fast forward and 0 is paused.\n\nIt reaches everything — the clips, the physics, the fixed loop, the follow camera, the argument `setAnimationLoop` is handed and `p.time` in a post body — because all of them are handed one delta rather than reading a clock of their own.\n\nNegative throws: nothing downstream of it can run backwards. `paused` is a read-only `timeScale === 0`; pause by writing 0 and resume by writing the scale you want back.",
		"three.clock.advance(seconds)": "Move the clock by hand, whatever the scale is — which is how a pause is single-stepped: `three.clock.timeScale = 0` and then `advance(1 / 60)` is exactly one frame of world, clips and bodies and fixed steps and `p.time` together.\n\nIt lands on the next frame rather than immediately, so under `--mcp` the order is `run_script`, a frame, screenshot. Two runs that ask for the same amount draw the same picture, which is what makes a screenshot with a post pass reproducible.\n\nCalls accumulate into one frame rather than queueing frames, and that is the one way this verb can lie to you: ten `advance(1 / 60)` before the next frame boundary is a single frame worth a sixth of a second, and the solver catches up at most five steps and drops the rest while the fixed loop does the same at eight. Measured: ten in one call moved the clock the full 0.167 s and dropped a falling body 1.41 units where ten stepped frames drop it about 2.8. The time lands and the world does not.\n\nTo step n frames, call it once per frame boundary — under `--mcp` that is n round trips.",
		"three.clock.fixedRate / three.clock.fixedDelta": "How many fixed steps a second of game time is worth — 60 by default, 1 to 240 — and the step that follows from it, in seconds.\n\nIt does not change the solver's rate, which is 60 Hz and is the solver's business: a script asking for 30 Hz gameplay must not quietly halve the accuracy of every contact in the scene.",
		"three.input.isDown(key)": "Whether a key is held right now. Poll this in the animation callback for continuous movement — a held key fires no repeat events.",
		"three.input.pressed(key) / released(key)": "Whether the key went down (or up) during the frame being drawn. Meaningful inside the animation callback; between frames it reports the last frame, which is almost always nothing.",
		"three.input.text": "What was typed this frame, as UTF-8, with modifier chords, control characters and the function-key range filtered out. The layout and the shift key are already applied, so this is what a text field wants rather than the key map.",
		"three.input.keys()": "Every key name there is. The same list the host searches, so it cannot be out of date.",
		"three.onKeyDown(key, fn) / three.onKeyUp(key, fn)": "Call `fn(keyName)` once when the key goes down (or up), from inside the frame.\n\nOne handler per key per edge — binding again replaces, null unbinds, and up to 32 exist at a time. Synchronous only, and stopped for good if it throws, exactly as the animation callback is.\n\nEscape is the host's under `--debug`: it closes the window whatever a script binds, so bind a pause menu to it and test the menu without that flag. In an ordinary run no key is reserved and escape is yours — the same is true of shift+R, which reloads only under `--debug`.",
		"three.input.pointer": "`{ x, y, dx, dy, inside, down, right, middle, clicked, scroll, scrollX }` — the whole mouse for this frame, as one reading. Read it in the animation callback.\n\n- `x` and `y` are in the rendered image's pixels counted from its top-left corner, which is what `scene.pick(x, y)` takes.\n- `dx` and `dy` are how far the cursor moved since the previous frame, in those same pixels — what a mouse look is built out of, and not the same thing as differencing `x` yourself, which answers with the frame before the one being drawn. The browser calls them `movementX`/`movementY`. They keep reporting while the cursor is outside the window, and without pointer lock they stop at the edge of the screen.\n- `scroll` is the wheel over this frame, positive away from the user (the opposite of the browser's `deltaY`), in notches or fractions of one from a trackpad, with `scrollX` the horizontal half. Both are zero on the frames nobody turned it.\n- `down`, `right` and `middle` are the three buttons as latches rather than edges; `clicked` is the one edge.\n- `inside` is false when the cursor has left the window, and everything is zero when there is no window.",
		"three.controls.enabled": "Whether the mouse still reaches the camera. True by default; false stops the drag, the right-drag pan and the wheel zoom, and stops the coast a flick leaves behind.\n\nIt is for a scene that drives the camera itself — a follow camera, a first-person look — which writes yaw, pitch and target every frame and would otherwise have the turntable writing them again from whatever the hand did. `three.camera.orbit()` and `three.camera.frameAll()` are unaffected, because a script writing the camera on purpose is the thing being enabled rather than the thing being stopped.\n\nA drag in progress is dropped rather than finished, and turning it back on waits for a fresh press instead of resuming the old one. It reads back what was written to it with no window open, and it survives `new three.Scene()` — following the camera rather than the background, because a game that took the mouse for its own camera should not lose it at every level.",
		"three.onClick(fn)": "Call `fn(hit, x, y)` once when the window is clicked, from inside the frame. `hit` is what is under the cursor — the same intersection `scene.pick(x, y)` answers with, or null for a miss — so click-to-select is one call.\n\nA click is a press and a release in the same place: dragging orbits the camera and does not fire this. One handler; binding again replaces, null unbinds. Synchronous only, and stopped for good if it throws.",
		"three.physics.add(object, options)": "Give an object a body and answer with the object.\n\nThe description is `object.body` if it has one and `options` wins over it, so a scene can be described once and tweaked at the call: `{ shape: 'box' | 'sphere' | 'capsule' | 'hull' | 'heightfield', mass: 1, friction: 0.5, restitution: 0.2, kinematic: false, trigger: false }`. Mass 0 means static.\n\nThe object has to be in the scene already — a body is placed at a world position — and has to be a child of the scene rather than of another object, because the solver works in world space and a parent transform would fight it. A group draws nothing and so has no size to take a collider from; give the body to a mesh.",
		"three.physics.remove(object)": "Take the body away, and answer whether there was one. A body removed while it is inside a trigger still emits its exit event, so a script that destroys something in a trigger volume still hears it leave.",
		"three.physics.gravity": "`[x, y, z]`, y-up, read and written as an array. Set once at boot; it is a world setting and not a transform, which is why it is not a live Vector3. It is per scene, like the world it belongs to — `scene.physics.gravity` for one that is not being rendered.",
		"three.physics.count": "How many bodies the rendered scene's world holds. `scene.physics.count` is any scene's.",
		"three.physics.velocity(object)": "`[lx, ly, lz, ax, ay, az]` — linear in world units per second, angular in radians per second — or null when the object has no body.\n\nBoth at once because a script that wants one usually wants the other; null rather than a throw because this gets asked in a loop over things that may or may not have bodies.",
		"three.physics.setVelocity(object, [x, y, z])": "Assign a body's speed, in world units per second. This is what a character uses: set it every frame from the keys that are down, because what you want is a speed.\n\nOnly a dynamic body can be given one — a static body's inverse mass is zero so nothing would happen, and a kinematic body is driven by the transform your script writes so it would be discarded a fraction of a step later. Both throw and say which.\n\nThe solver recomputes the velocity from what actually happened at the end of every step, so this survives one integration by design.",
		"three.physics.setAngularVelocity(object, [x, y, z])": "Radians per second about each world axis; the vector's length is the rate. Same dynamic-only rule as `setVelocity`.",
		"three.physics.applyImpulse(object, [x, y, z], at)": "A push, in mass times velocity — so the same impulse moves a heavy thing less, and this is what a jump, a bat or an explosion wants rather than `setVelocity`. It adds to whatever the body was already doing.\n\n`at` is optional and is an offset from the body's centre in world axes, not a world position: give one and the push tumbles the body as well as shoving it.\n\nA sleeping body is woken first, so a settled crate and a rolling one answer the same push the same way.",
		"three.physics.applyTorqueImpulse(object, [x, y, z])": "A spin with no shove, so \"make this rotate\" does not mean solving for an offset and a force that happen to produce the spin you wanted.",
		"three.physics.joint(a, b, options)": "Bolt two body-backed objects together, and answer with the joint's id — which is what `removeJoint` takes, because a joint is not a node and has nothing else to be named by.\n\nA joint is a list of limits: some of the joint frame's axes, held to some range. That is glTF's description of one (KHR_physics_rigid_bodies) and it is what the solver stores, so a limit read out of a `.glb` and a limit written by hand are the same object.\n\n```js\n{ limits: [{ linearAxes: [0,1,2], angularAxes: [0,1,2], min, max, stiffness, damping }],\n  axis: [x, y, z], pivot: [x, y, z], stiffness, damping, collide }\n```\n\n- Axis 0 is `axis` (default `[0, 1, 0]`); axes 1 and 2 are derived perpendicular to it, so a limit naming `[1, 2]` means \"the plane the axle is normal to\" whichever way the axle points.\n- No range is a lock. A limit with no `min` and `max` holds its axes at zero, which is glTF's default for both and the opposite of what \"no limit\" sounds like. A hinge is therefore `limits: [{ linearAxes: [0,1,2] }, { angularAxes: [1,2] }]` — everything held but the axle.\n- A mask of several axes becomes one limit per axis, which agrees with the file format exactly for a lock and differs for a range: a box rather than glTF's cone.\n- The joint is made where the bodies are — the pivot defaults to halfway between the two centres and the relative orientation is whatever they are turned to right now — so place both objects and then join them.\n- `stiffness` is a spring constant and 0 is the rigid end: the solver reads it as `(1 / stiffness) / dt²` and special-cases 0 to no give, so bigger is stiffer and something felt on a one-kilogram prop is in the low thousands. `damping` is carried into the joint and is not read by the solver yet; it is accepted so a limit out of a file survives.\n- At least one end has to be dynamic. `collide` is false by default, because two things bolted together usually overlap where they are bolted.\n- A linear axis is a world direction and does not turn with the bodies, so a slider on something that rotates is not expressible; angular axes do turn with them.",
		"three.physics.joint (the four shorthand types)": "`type` names one of four limit lists, expanded in the prelude, so anything it can say can also be said by hand.\n\n- `'fixed'` welds — every linear and angular axis held. The default.\n- `'point'` is a ball and socket — the three linear axes held and nothing else.\n- `'hinge'` turns about axis 0 — linear held, angular `[1, 2]` held.\n- `'slider'` runs along axis 0 — linear `[1, 2]` held, angular all held.\n\n`range: [min, max]` bounds the one free axis a hinge or a slider has. It is refused on `fixed` and `point`, which have none, and `[0, 0]` is refused because it would lock that axis and weld the joint shut rather than bound it.\n\nGive `limits` instead and `type` and `range` are ignored — the list is the joint.",
		"three.physics.removeJoint(id)": "Let one joint go, by the id `joint()` answered with. False when the id names nothing, so removing twice is not an error.\n\nRemoving either body removes the joints holding it, so a script that destroys things does not have to track their joints as well.",
		"three.physics.soft(object, options)": "Simulate this object's own vertices as particles held together by the mesh's edges, and answer with the object. `{ mass: 1, softness: 0, damping: 0.99, volume: false, bending: false, friction: 0.5, restitution: 0.2 }`.\n\nThere is no `shape`: a soft body has no collider, because the thing that collides is the drawing. How it deforms is decided by how the mesh was modelled, and a denser mesh is a more expensive and more detailed one.\n\n- `softness` is XPBD compliance on the links; 0 cannot stretch.\n- `volume` holds the enclosed volume, which is a balloon rather than a bag.\n- `bending` resists folding, which is what stops cloth creasing flat. Each takes true or a compliance number.\n\nTwo things follow that a rigid body does not have. Its transform is the solver's — `object.position` reads where the particles average out to and writing it throws, so a script pushes one with `pin()` and nothing else. And it costs a draw call of its own: two copies of one BoxGeometry are one draw, but two soft ones are two, because each is writing its own vertices.\n\nIt is refused on a skinned mesh, whose vertices already belong to its skeleton.",
		"three.physics.removeSoft(object)": "Take the soft body away, and answer whether there was one. The mesh goes back to the shape it was modelled as, where the last step left it, and the transform is yours again.",
		"three.physics.points(object, into) / softCount(object)": "The particles, in world space, as a Float32Array of `count * 3` — and how many there are.\n\nOne particle per distinct point in the mesh, not one per vertex: a BoxGeometry has twenty-four vertices and eight particles, because a box that shades with hard edges stores each corner three times and the solver has to treat those as one or the box falls into six loose squares.\n\nThis is how a script finds the index to pin — read them and pick by position, since a particle has no other name. `into` is optional and lets you reuse one array across frames, the way `texture.read` does.",
		"three.physics.pin(object, particle, at) / unpin(object, particle, mass)": "Hold one particle at a world point — or where it already is, with no third argument — and let it go again.\n\nThis is the only way to push a soft body: it has no velocity to set and no centre to shove. A pin is absolute and is reapplied after every substep, so a pin moved each frame carries the body with it, which is how a soft thing is dragged, thrown or attached to a hand.\n\n`unpin`'s `mass` is what the particle weighs afterwards, and defaults to the body's own per-particle share.",
		"object.body": "What kind of body `three.physics.add` would give this object, and what it gave it: `{ shape, mass, friction, restitution, kind }`. Set it yourself to describe one, or read it back after `add` to see the defaults filled in. Null once the body is removed.",
		"three.onTrigger(fn)": "Call `fn({ type: 'enter' | 'exit', trigger, other })` when a trigger body starts or stops overlapping something, from inside the frame. `trigger` and `other` are the objects, or null for one whose node has already gone.\n\nOne handler; binding again replaces, null unbinds. Synchronous only, and stopped for good if it throws — the same rules `onClick` follows.",
		"three.onContact(fn)": "Call `fn({ type: 'start' | 'end', a, b, normal, point })` when two bodies touch or come apart. Unlike a trigger, a contact also produced a physical response.\n\n`normal` and `point` describe the touch and mean something only on a start — by the end there is no contact left to describe. Same registration and same rules as `onTrigger`.",
		"three.camera.attach(object, options)": "Follow an object with the camera.\n\n- `{ offset: [x, y, z] }` is added to its world position and becomes the orbit point every frame.\n- `{ distance }` is how far behind the eye sits; 0 puts the eye on the point, which is first person.\n- `{ lag }` is seconds of catch-up — 0 for rigid, ~0.12 for a camera that trails.\n\nThe follow runs last in the frame, after the animation, the solver and your callback, so the camera is never a frame behind what it is watching.\n\nIt owns the orbit point and nothing else: a drag still orbits, the wheel still zooms, `orbit()` still aims, and a pan stops working because a pan writes the orbit point. The offset is world space, so a camera bolted into something that rolls is not expressible. `frameAll()` throws while attached rather than being undone a frame later.",
		"three.camera.detach() / three.camera.attached": "`detach()` stops following and answers whether it was, leaving the camera exactly where the last frame put it.\n\n`attached` is the object being followed, or null — and null is also how you find out that what you were following was destroyed, because the host drops the attachment silently rather than throwing from inside a frame nobody called.",
		"three.camera.orbit(yaw, pitch, distance)": "Degrees, degrees, world units. Any argument may be omitted to leave it alone.",
		"three.camera.lookAt(x, y, z)": "Point the turntable at a world position.",
		"three.camera.frameAll()": "Aim at everything in the scene and back off far enough to see it.",
		"three.camera.position()": "The camera eye, in world space — the point a boom of `distance` puts it. Not `target` (what it orbits) and not writable.\n\nThis is the fix for \"which way is the camera looking\": you cannot read it from the camera itself otherwise, because `camera.position` and `getWorldPosition()` do not exist.\n\nThe convention: yaw is degrees about +Y from +Z, so `orbit(0,0)` puts the camera at +Z looking toward -Z and `orbit(90,0)` at +X looking toward -X.",
		"three.camera.forward()": "Where the camera looks, as a unit Vector3 in world space, pitch included.\n\nCompute the camera-relative move frame as `forward()` and `right()` rather than by hand — the signs are easy to get wrong and read back as the character walking sideways.",
		"three.camera.right()": "The camera's right on the ground plane, unit Vector3, y=0 — the \"D\" key. Flattened so it stays a strafe direction while the camera is pitched down at a character.",
		"three.camera.planarMove(fwd, strafe)": "The world-space direction for a camera-relative input: `fwd` is the W/S axis (+1 to -1), `strafe` the D/A axis (+1 to -1). Returns a unit Vector3 (y=0), or the zero vector for 0,0.\n\nOne call that keeps a character glued to the camera's forward line instead of drifting sideways:\n\n```js\nconst v = three.camera.planarMove(fwd, strafe);\nplayer.position.x += v.x * speed * dt;\nplayer.position.z += v.z * speed * dt;\n```",
		"three.camera.near / three.camera.far": "Where the depth range starts and ends, in world units. Read-only: both are derived, from the orbit distance and from the scene's own bounds, every time the camera moves.\n\nThey are worth reading when something has stopped being drawn — geometry beyond `far` is not dim, it is absent, and it is culled as well as clipped, so `stats().culledLastFrame` moves too.",
		"three.light.direction / three.light.color / three.light.intensity": "The sun — light zero, and the only one that casts a shadow.\n\n`direction` is a world-space surface-to-light vector — the way a face has to point to be fully lit — and is a live Vector3, so `three.light.direction.y = -1` writes through. It is not normalized, so it reads back as you wrote it, and a zero one throws rather than making every shaded pixel a NaN.\n\n`color` takes a hex, a triple or an `{r,g,b}` and answers with the triple; `intensity` multiplies it and is how a light goes brighter than white. Defaults to `[0.35, 0.8, 0.45]`, white, and 1.",
		"three.light.ambient": "The floor a face turned right away from every light gets, 0 to 1: at 0 it is black, at 1 there is no shading at all and everything is its own flat colour. On `three.light` rather than on each light because it is not a light. Defaults to 0.25.",
		"three.light.set(direction, ambient)": "The sun and the floor at once. `ambient` may be omitted to leave it alone, and it is the floor rather than a colour — `three.light.color` is how the sun is coloured. There are up to four lights; `three.lights` is the list.",
		"three.lights": "The list of lights, four slots, the sun in the first — `three.lights[0] === three.light`. `length` is how many are lit and `max` is how many there can be.\n\n`add(direction, color, intensity)` fills the next slot and answers with it, and also takes `add({ direction, color, intensity })`; it throws once four are lit rather than dropping the fifth.\n\n`remove(i)` or `remove(light)` takes one out and closes the gap, exactly as `Array.splice` does, so an index held across a remove names a different light. Light zero cannot be removed — it is the one the shadow map is fitted around — so turn it off with `three.light.intensity = 0`.\n\nIt is iterable: `for (const l of three.lights)`. Only light zero casts; the rest light and do not shadow.",
		"three.light.shadow": "The shadow this light casts, off until you ask. `three.light.shadow = true` turns it on; `three.light.shadow = { enabled: true, size: 4096 }` sets several at once; and the five properties read and write one at a time.\n\n- `enabled` — whether the pass runs.\n- `size` — texels per side, clamped to 256..8192 and rounded down to a power of two, so it reads back as what will be allocated rather than as what you typed.\n- `bias` — an extra depth offset in the light's clip space, 0 by default, because each sample is already lifted two texels along its own normal. Reach for it in small numbers like 0.0005 if a scene still shows stripes.\n- `intensity` — how dark, 0 to 1. 1 takes the whole directional term away and leaves `three.light.ambient`, so a shadow is never black unless the ambient floor is.\n- `distance` — how far down the view direction the map is fitted, in world units. 0 (the default) means five times the camera's own orbit distance, derived every frame.\n\n`distance` is the sharpness knob and it beats `size`: the map covers a square this wide, so halving it is worth quadrupling `size` and costs nothing. Try roughly the distance shadows are worth having, and watch for the line across the ground where they stop.\n\n`three.light.shadow.fit` reads back where the map actually landed: `{ live, center, extent, near, far, texel }`, in world units, for the last frame. Read `extent` first — `extent / size` is the world size of a texel, and that is what decides whether an edge reads as a shadow or as a staircase. `live: false` means no pass ran.\n\nNothing is allocated and no shader compiled until the first frame with it on; turning it off costs nothing and keeps the map for next time; and activating a scene — which `new three.Scene()` does — turns it off.\n\nEverything opaque casts and everything shaded receives: there is no `castShadow` or `receiveShadow`, because two copies of one mesh disagreeing about it would be two draw calls. Glass casts nothing and neither do debug helpers.\n\nThere is one map, fitted around the whole scene every frame, so blocky shadows mean a large scene rather than a small map. While the pass is on the camera frustum stops culling, because a caster you cannot see still throws a shadow into the frame — so `stats().culledLastFrame` reads 0 and `stats().shadowDraws` is what the pass cost.\n\nThe way to make that pass cheap is `object.static = true` on whatever will not move again: static casters go into the map once and are kept, and each frame after draws only the movers. It costs no draw call in the colour pass. See the static-casters topic.",
		"material.repeat / material.offset": "How the map is laid across a surface. `repeat` is `[u, v]` — or one number for both — and is how many times the image is tiled; `offset` is `[u, v]` in whole repeats, for shifting it.\n\nWithout this a surface maps its texture exactly once, so texel density is a function of how big the mesh is and a 128px image across 100 units is a smear.\n\nOn the material, not on the texture as in Three.js: textures here are deduplicated by content across every file, so a transform on the texture would change every unrelated surface that used the same picture. Two densities of one image is two materials, which is what they already had to be.\n\nA repeat of zero throws — it maps the whole surface onto one texel.",
		"mapped_normal(s, texel) — normal maps in a ShaderMaterial": "A tangent-space normal map applied to a surface that carries no tangents. In a fragment body:\n\n```slang\nfloat3 n = mapped_normal(s, bumps.Sample(s.uv).rgb);\nreturn standard(s, s.albedo, n);\n```\n\n`bumps` is one of the material's declared textures, and `texel` is the map's rgb exactly as sampled — the decode from [0,1] to a direction happens inside, which is why the map has to be loaded with `{ colorSpace: three.LinearSRGBColorSpace }`.\n\nNo mesh here has a TANGENT stream and there is nowhere to put one, so the frame is rebuilt per pixel from screen-space derivatives of the world position and the uv. That is what makes it work on any textured mesh, including a generated PlaneGeometry, and it is also its two limits: a mirrored uv island comes out mirrored rather than flipped, and a face with degenerate uvs gets the interpolated normal back unchanged.\n\nFragment stage only — calling it from a vertex body is a compile error, because there are no derivatives there.\n\nA roughness map has no equivalent yet: a roughness or metalness map loads correctly and in the right colourspace, but there is nothing built in to feed it — a body is free to use one for whatever it likes.",
		"new three.MeshLambertMaterial({ map, side })": "The built-in shader with an image. Compiles nothing, needs no Slang, and cannot fail with a shader diagnostic — this is the way to put a picture on a shape. With no map it is a side and nothing else, which is the cheapest skydome.",
		"material.map": "The base colour image, or null. The material's map wins over whatever texture the mesh itself carries, so a glTF's own image can be overridden and cannot silently override yours.\n\nA mesh with no uvs shows nothing: every parametric shape and every glTF mesh has them, a ConvexGeometry does not — on one of those the map is set, correct, and invisible.",
		"new three.ShaderMaterial({ fragment, uniforms })": "Compile a fragment function into a material. Uniforms are at most 68 bytes in total (17 floats); each is a number or an array of up to four numbers.",
		"mesh.material": "Assign a MeshLambertMaterial or a ShaderMaterial, or null for the default shader. Meshes sharing a mesh ref and a material are one draw call; giving two of them different materials makes two.",
		"material.dispose()": "Give back the reference this handle holds, and with it the pipeline the material was compiled into.\n\nNot a free: the material goes when no mesh names it either, so disposing while a mesh still draws with it leaves that mesh correct and collects the material when the mesh goes.\n\nCall it on a ShaderMaterial you are done with — an agent iterating on a shader compiles a new pipeline every run, and without this they accumulate for the life of the process. Disposing twice does nothing; using a disposed material throws. The default and line materials are shared and cannot be disposed.\n\n`stats().materials` is how you watch it work, and the host prints a line naming the count once it passes 64.",
		"material.uniforms.<name>": "Read or write a uniform. Writing takes effect on the next render.\n\nOnly names declared at construction exist; assigning to any other name throws. A uniform declared as a table is written a row at a time — `material.uniforms.palette[1] = [0, 1, 0]` — or all at once.",
		"mesh.color": "This copy's own tint, multiplied into albedo. `[r, g, b]`, `[r, g, b, a]`, `{r, g, b}` or a hex number like `0xff8800`.\n\nCosts no draw call: copies of one mesh may all differ. Works with no material at all, and reaches a `shade()` body as `s.color` with albedo already tinted.\n\nThe fourth channel fades this copy when — and only when — its material was built transparent: it multiplies the material's own opacity, so one of a thousand copies sharing a draw call can be half there while the rest are solid. On an opaque material the alpha is discarded by the pipeline and changes nothing.",
		"mesh.variant": "Which row of the material's uniform table this copy draws with, as `s.variant` in the body. Costs no draw call either.\n\nZero and meaningless until the material declares a table; past the end it is clamped to the last row rather than reading rubbish.",
		"three.NoBlending / three.NormalBlending / three.AdditiveBlending": "The values `material.blending` takes — 0, 1 and 2, Three.js's numbers again.\n\n`NormalBlending` is what `{ transparent: true }` means and is what glass, water and a foliage card want. `AdditiveBlending` never darkens what is behind it and is what fire, a glow and a beam want.\n\nBoth are decided when the material is constructed and neither can be assigned afterwards: this device bakes blending into the pipeline, so changing it is building another material, which is one line.\n\nThree things follow, worth knowing before a scene is built on them:\n\n- Transparent draws are sorted farthest-first against the near plane and drawn after every opaque one, so glass shows the wall behind it.\n- Copies inside one instanced bucket are not sorted against each other — they are one draw call, and the depth order within it is whatever the vertex order is. Three.js's per-object sort has the same limit, and the fix in both is to space the panes out or split them.\n- A transparent frame may issue more draw calls than `stats().drawCalls` reports, deliberately: depth interleaving splits buckets and the split depends on where the camera is, so `stats()` answers what the scene costs rather than what this angle cost. The number is a floor, never an over-estimate.",
		"three.FrontSide / three.BackSide / three.DoubleSide": "The values `material.side` takes — 0, 1 and 2, the same numbers Three.js gives them.\n\n`BackSide` keeps the back faces, which is what makes a sphere visible from inside: it is how a skydome is built, and scaling one by -1 instead does nothing, because a negative scale does not reverse a triangle's winding.\n\n`DoubleSide` keeps both and is what a plane seen from either direction wants — a flag, a leaf card, a piece of a wall you can walk past.",
		"texture.read(into)": "The pixels, copied back off the device: a Uint8Array of `width * height * 4` RGBA bytes.\n\nThe bytes that went in, not the ones the shader sees — the copy converts nothing, so a DataTexture reads back byte-for-byte identical to the array it was built from and a PNG reads back as its own pixels; the sRGB decode happens at sample time and is not in here.\n\n`into` is optional and lets you reuse a buffer. This copies off the device and waits for the queue, so it belongs at load or in a test rather than in a frame. It is also what makes a texture testable and what lets `scene.export` write a generated one.",
		"three.texture(path, options)": "Decode a PNG, JPEG or KTX2 and upload it, answering with a Texture. Synchronous.\n\nA KTX2 may hold Basis (ETC1S or UASTC, which is what KHR_texture_basisu means) or an ordinary Vulkan format, compressed or not; either way it arrives as RGBA8, and a `.glb` whose textures are KTX2 loads with them. The format comes from the file's first bytes rather than its name.\n\nDeduplicated by the decoded image, so the same picture reached by two paths — or by a path and a `.glb` — is one upload, and `three.stats().textures` counts it once.\n\n`options` is `{ colorSpace, generateMipmaps }` and nothing else; an unknown key throws rather than being ignored, so a Three.js line carrying `magFilter` or `wrapS` is told so instead of quietly doing something different.",
		"three.SRGBColorSpace / three.LinearSRGBColorSpace / three.NoColorSpace": "Which space a texture's bytes are in, passed as `three.texture(path, { colorSpace })`. This is the difference between a colour map and a normal map.\n\n- `SRGBColorSpace` is the default and is right for anything an artist looked at while making it — a base colour, an albedo, a photograph.\n- `LinearSRGBColorSpace` is for a map whose channels are numbers rather than colours: a normal map's xyz, a roughness or metalness or occlusion map, a height field, a lookup table.\n- `NoColorSpace` is Three.js's other spelling of linear and is the same image here.\n\nNeither mistake reports an error: a colour map loaded linear is washed out and reads as a lighting bug, and a normal map loaded sRGB goes soft and reads as a bad bake.\n\nThe colourspace is part of a texture's identity, so the same file loaded both ways is two uploads on purpose.",
		"new three.DataTexture(data, width, height, options)": "Upload pixels a script generated. Rows run bottom-to-top, four bytes per pixel.\n\nThe bytes are read and copied inside the call, so the array is yours again immediately. Wrong byte counts are refused with the arithmetic in the message rather than uploaded skewed.\n\n`options` is `three.texture`'s; a generated lookup table wants `{ colorSpace: three.LinearSRGBColorSpace, generateMipmaps: false }`.",
		"texture.levels and texture.generateMipmaps": "How many mip levels the image got, and whether that is more than one.\n\nA full chain is built by default, which is what stops a textured floor shimmering as the camera moves — without one, every sample comes from the full-resolution image however few pixels the surface covers.\n\nBoth are read back off the upload rather than echoed from what you asked for, so a device that cannot filter the format reports false here having been passed true. Pass `{ generateMipmaps: false }` for pixels meant to be indexed exactly rather than sampled at a distance.",
		"texture.dispose()": "Give back the reference this handle holds. Not a free: the image goes only when nothing names it, so disposing while a material still draws with it leaves that material correct. Disposing twice does nothing; using a disposed texture throws.",
		"three.setPost({ fragment, uniforms, textures })": "Run one shader over the whole finished frame. `fragment` is a Slang function `float3 post(Post p)` returning linear rgb.\n\n`Post` carries:\n\n- `color` — this pixel of what ran before this pass, already decoded to linear. The rendered scene, for the first pass of a chain.\n- `scene` — this pixel of the rendered scene whatever has run since; equal to `color` on the first pass.\n- `uv` — 0..1 across the frame, (0,0) top left.\n- `resolution` — the frame in pixels. `1.0 / p.resolution` is one texel, which is what a blur steps by.\n- `time` — seconds since this shader was set, on the game clock, so a paused world has a still chain.\n- `depth` — how far this pixel is from the camera, in world units along the view direction. Already linearized, which is the point of it: the raw device depth spends half its range on the first few percent of the frustum, so fog, depth of field and distance-aware edges written against it bunch up against the camera. A pixel nothing was drawn into reads as the far plane.\n- `tap` — another pass's output, which only `three.addPass` can fill. See its `reads`.\n\nThere are no normals and no motion vectors.\n\n`p` gives you this pixel; the two images behind `color` and `scene` are also in scope as samplers named `prev` and `scene`, so a body that needs the neighbours reads `prev.Sample(p.uv + off)` — which is what the texel step is for, and the whole of how a blur is written.\n\nEach uniform is readable in the body by its own name. They are at most 104 bytes in total (26 floats), each a number or an array of up to four numbers, and not a table — a post pass draws one triangle over the whole frame, so there are no instances for a row to belong to.\n\n`textures` is a ShaderMaterial's: `{ grade_lut: tex }` declares a Sampler2D the body reads by that name, up to four, with no binding number written anywhere. They are what a frame cannot supply about itself — a ramp to grade through with `grade_lut.Sample(float2(p.color.r, 0.5))`, a noise field to distort or dither by, a mask that says where the effect applies. Tile one by the frame rather than by uv, `p.uv * p.resolution / 256`, or it stretches with the window. A sampler you leave null reads white.\n\nIt compiles on the call, so a bad body throws here carrying the Slang diagnostic with `post:<line>` counting the lines you wrote; a failed set leaves the previous chain running, so it is the old shaders or the new one and never neither. Needs a GPU device.\n\nIt applies identically to the window, to `three.render()` and to every screenshot — there is one recording path and the branch is inside it. `post()` returns rgb and never alpha: a screenshot forces alpha opaque anyway, so a body that could dim it would make the window and the file disagree.\n\n`setPost` replaces the whole chain (the old pipelines are retired for you) and `three.addPass` adds to it. The chain belongs to the renderer rather than to the scene, so it survives `new three.Scene()` and outlives the script that set it. `three.setPost(null)` is the only thing that clears it.",
		"three.addPass({ fragment, uniforms, textures, reads })": "Put another full-screen pass at the end of the chain. The same spec `three.setPost` takes and the same handle back; the difference is what the body reads.\n\n`p.color` is what the pass before this one wrote, and `p.scene` is the frame as the geometry left it. Those two are the dependency model, and between them they cover most of what a multi-pass effect wants: bloom is `blur(bright(scene)) + scene`, which is `p.scene` three passes later.\n\n`reads` is the third source, for what they do not cover: hand it the handle an earlier `addPass` gave you and that pass's output arrives as `p.tap`.\n\n```js\nconst bright = three.addPass({ fragment: threshold });\nthree.addPass({ fragment: combine, reads: bright });\n```\n\nThat is the pass in the middle of a chain that two later passes both want, or a mask one pass built for another to apply. The sampler is in scope as `tap_image` (not `tap`, which is a word bodies use) for reading a different pixel of it.\n\nIt costs the tapped pass an image of its own — the chain ping-pongs two between passes and a tapped one cannot be overwritten — so it is one allocation per distinct pass tapped and nothing for a chain that taps none.\n\n`reads` must name a pass already in the chain; a later index or a cycle throws. It belongs on `addPass`, never on `setPost`, which is always the first pass and has nothing before it. Leave it out and `p.tap` is the rendered scene, the same as `p.scene`.\n\nEverything between passes is linear float rather than 8-bit, so a pass may return values above 1 and the next one still sees them; the display encode happens once, after the last pass.\n\nAdding to an empty chain is exactly a `setPost`. It does not invalidate handles you already hold — earlier passes keep their index and their shader — which is what lets a script animate every pass at once. There is no `removePass`: dropping one out of the middle would renumber the handles after it, and a `setPost` followed by the `addPass` calls you want is the same effect said in a way that cannot leave a handle pointing at somebody else's shader.",
		"the handle three.setPost() and three.addPass() answer with": "`{ fragment, index, uniforms, textures }` — the body that is running, where in the chain it runs, and live uniforms and textures objects exactly like a material's.\n\n`post.uniforms.gain = 2` is a 4-byte write that takes effect on the next frame with no compile and no pipeline, which is what makes an animated post pass free, and `post.textures.grade_lut = other` swaps an image the same way.\n\nOnly names given at the call exist; assigning any other throws. A later `setPost` replaces the whole chain, and writing through a handle from before it throws rather than steering whatever is at that index now. `addPass` leaves earlier handles working.",
		"three.debug.write(...values)": "How a script answers with a value. Every argument becomes one entry in the `debug` array of the result, as JSON rather than as text inside the log — so an object stays an object instead of arriving escaped in a string.\n\nIt can be called from wherever the number was worked out rather than gathered into one expression at the last line, and it works inside a system or an animation callback. On the command line it prints as `debug: [...]`.\n\nEntries written in a frame are held and reported by the next run, the way `console.log` from a callback is. A value with no JSON form — a cycle, a function, undefined — arrives as the text `String(value)` would have shown rather than punching a hole in the array.\n\n```js\nthree.debug.write({ crates: Crate.count, wumpa: Wumpa.count });\n```",
		"three.debug.overlay(text)": "One line over the top-left of the frame, and one entry in the run's `debug` array as `{ overlay: \"...\" }` — so a person and an agent read the same line, and a screenshot carries it, because the text is drawn into the offscreen target the same as the scene is.\n\nIt lasts one frame: set it again each frame to keep it up, which makes a HUD one line and lets a one-shot note clear itself.\n\n```js\nthree.frame(() => three.debug.overlay(`hp ${p.hp}`));\n```\n\nText only, no layout and no widgets, so nothing here can swallow a click.",
		"three.ui.set(tree)": "The interface, as one description: a tree of plain objects, each with a `type`.\n\n- Layout — `column`, `row`, `stack`, `padding`, `grid`, `clip`, `anchored`, `scroll`.\n- Painted — `rect`, `label`, `draw`.\n- Interactive — `button`, `checkbox`, `slider`, `select`, `tree`, `textfield`.\n\nChildren go in `children: [...]` or `child: {...}`, and a falsy child is skipped, so `cond && {...}` is how a row is conditional.\n\nColours take a hex number, an `[r, g, b]` or an `[r, g, b, a]`; `radius` and insets take one number, two, or four in cui's order — insets `{left, top, right, bottom}` and radius `{TL, TR, BR, BL}`. `size` is per axis and 0 means take what you are offered.\n\nHandlers are `onClick`, `onChange`, `onCommit`, `onSubmit`, `onSelect` and `onToggle`, and they must be synchronous, because a handler runs inside the frame and the frame does not wait.\n\nIt is drawn over the finished frame into the same image a screenshot reads, so an agent sees the interface a person sees. `three.ui.set(null)` takes it down.\n\nCall this when the shape changed and `three.ui.patch` when a value did: the tree is retained and a node that did not change costs nothing to redraw, so rebuilding it every frame is the one thing worth not doing.\n\nThe other door is `three.Widget`, which does that bookkeeping for you: `render()` describes the interface as it is now and what reaches the host is the difference. The two are exclusive — this verb throws while a widget is mounted — because the last one through would win every frame.",
		"three.ui.patch(key, props)": "One keyed node, one or more of its values — the verb a HUD uses.\n\n```js\nthree.frame(() => three.ui.patch('fps', { text: `${fps | 0} fps` }));\n```\n\nA node carries a `key` in the snapshot that named it, and the key lives until the next `three.ui.set`. Patching one that names nothing throws rather than doing nothing, because a HUD that silently froze is the failure that costs an afternoon.\n\nThe fields are the ones that are values rather than structure: `text`, `value`, `checked`, `selected`, `offset`, `disabled`, `color`, `min`, `max`, `size`, plus `ops`, `options` and `rows`, which replace a list wholesale. Anything that would change the shape of the tree is a `set`.",
		"three.ui.draw(ops)": "The screen-space layer: a list of drawings positioned in frame pixels. A crosshair, a health bar, a damage flash and a minimap are all this, and none of them is a widget.\n\nSeven ops, which are the whole drawing surface the built-in widgets paint with:\n\n- `rect` `{at, size, color, radius, borderColor, borderWidth}`\n- `circle` `{center, radius, color}`\n- `ellipse` `{center, radii}`\n- `line` `{from, to, thickness, color}`\n- `arc` `{center, radius, start, sweep, thickness, color}` — radians, 0 at +x, turning clockwise\n- `text` `{at, text, size, color}`\n- `shadow` `{at, size, blur, color, radius}`\n\nCoordinates are the same top-left image pixels `three.input.pointer` arrives in, so a reticle at the cursor needs no conversion.\n\nIt is a `three.ui.set` — one draw node filling the frame — so it replaces the interface. To put drawings beside widgets, use `{ type: 'draw', size, ops }` inside the tree, where the coordinates are then the node's own and `onClick` makes it clickable.",
		"three.ui.measure(text, options)": "What a string will take, as `[width, height]` in pixels. `options` is `{ font, size }`.\n\nDrawing text by hand is arithmetic without it — centring a readout inside an arc is a measured width — and this is the same measurement the renderer makes when it lays the glyphs down, so positioning by it lands where they go.",
		"three.ui.slot": "Where a native plugin has offered to let a script draw, if one has: `{ present, generation, x, y, width, height }`.\n\nA host loaded with `--plugin` can name an element of its **own** interface as the place a snapshot goes. With one offered, `three.ui.set` lands in that pane and the application around it stays on screen; without one — no plugin, or a plugin that has offered nothing — `present` is false and `set` behaves as it always has, taking the frame.\n\nThe snapshot does not survive the slot. The pane can be closed or its tab switched, and what was in it is recycled, so a panel written against an editor watches the generation and draws itself again:\n\n```js\nlet seen = -1;\nthree.systems.frame('panel', () => {\n  const slot = three.ui.slot;\n  if (!slot.present) return;\n  if (slot.generation !== seen) { seen = slot.generation; three.ui.set(panel(slot)); }\n  three.ui.patch('count', { text: `${n}` });\n});\n```\n\n`width` and `height` are the pane's, in the same points every other number here is in, and are zero for the one frame between the plugin offering the slot and the interface laying it out.\n\n`x` and `y` are the pane's top-left corner in window points. They are there for one job: `three.input.pointer` is the **window's** and knows nothing about panes, so a panel that draws its own controls with `three.ui.draw` subtracts them to hit-test.\n\n```js\nconst p = three.input.pointer;\nconst local = { x: p.x - slot.x, y: p.y - slot.y };\n```\n\nWidgets in a snapshot never need this — the interface lays them out and hit-tests them. The pointer reading is global, so it is true through a menu the host has painted over the pane; a `draw` node does not own the pointer yet.",
		"three.ui.clear()": "Takes the interface down, mounted widgets included.\n\n`three.debug.overlay` is not part of it and stays: the debug line and a script's interface are two floors of one root, with the line always on top.",
		"three.ui.flush()": "Re-render every `three.Widget` whose state has changed, now, instead of when the frame reaches the `ui.widgets` system.\n\nA script only needs it to look at an interface without drawing a frame first — a test, or a screenshot taken straight after a mutation.",
		"three.debug": "What the renderer draws instead of the scene, when you ask. `three.debug.view` is `'off'` (the default), `'shadow'` or `'shadowMap'`.\n\nIt exists because a frame with no visible shadows has three explanations — the pass did not run, the pass ran and the map is fitted somewhere else, or everything in shot is genuinely inside one big shadow — and until this existed the renderer distinguished none of them.\n\n- `'shadow'` draws how much light reaches each surface as greyscale: white is lit, black is fully shadowed. Uniformly white is a pass that did not run or a fit nothing landed in; uniformly dark usually means the sun is low enough that a wall is shadowing the whole scene.\n- `'shadowMap'` draws the depth the lookup reads at each surface, near-to-far greyscale — the map itself, seen through the geometry that samples it. Magenta is outside the fitted box, so a mostly-magenta frame is a `three.light.shadow.distance` fitted somewhere other than where you are looking; dark purple is no shadow pass this frame.\n\nThe sky is not a surface and has no shadow, so a debug view colours only what the geometry covers.\n\nIt is deliberately not scene state: `new three.Scene()` does not clear it, for the same reason it does not move the camera. Pair it with `three.light.shadow.fit` for the numbers, and with `--frames` if you are running headless."
	},
	"stats": {
		"drawCalls": "vkCmdDrawIndexed calls for one frame of this scene.",
		"uniqueMeshes": "Distinct (asset, mesh) pairs drawn.",
		"instances": "Total placed meshes. A thousand of these can be one drawCall.",
		"nodes": "Live nodes, groups and the root included.",
		"scenes": "How many Scenes exist. `new three.Scene()` shows a new world without freeing the one before it, so this is the number a level transition has to bring back down. Each extra scene holds a node pool, a physics world, a nav bake and asset references — invisible in every other number here, since a scene nobody looks at costs the frame nothing and memory everything. `three.scenes` is the list behind the count; `three.disposeInactive()` frees all but the rendered one. A warning arrives once the count starts doubling past four.",
		"colliders": "Of those, the meshes in the spatial index — what every sweep and raycast tests a box against. Drawable meshes are collision geometry by default, so this grows as a level is decorated whether anyone meant it to or not; `object.collides = false` takes one out. Read it beside `nodes`: a large gap plus slowing sweeps is a field of grass acting as a fence.",
		"assets": "Loaded files and generated shapes resident on the device. Watch it across `scene.unload()`.",
		"triangles": "Summed over instances, so 1000 copies of a 500-triangle mesh is 500000.",
		"vertices": "Likewise.",
		"textures": "Unique images on the device, deduplicated by content across every loaded file.",
		"textureBytes": "What those cost.",
		"geometryBytes": "Every vertex stream and index buffer on the device, plus the positions and triangle indices kept resident on the CPU for the pick tree. Both halves, because a few hundred thousand vertices is tens of megabytes on each side. Falls to zero across a full unload.",
		"targetBytes": "The offscreen frame: one colour and one depth image at the render size. About 17 MB at 1080p, held before a single mesh loads, and the floor under everything else here.",
		"postBytes": "The post chain: Image A, as much of the ping-pong pair as the chain needed, and one image per tapped pass, at eight bytes a pixel against the target's four — so a two-pass chain at 1080p is around 40 MB. 0 until a post shader is first set, and a high-water mark after that: `three.setPost(null)` retires the shaders and keeps the images for the next chain at the same extent. A nonzero reading with nothing running is that, not a leak.",
		"shadowBytes": "Two D32 images at `size` squared once anything in the scene is static, one before that. 2048 is 34 MB, 4096 is 134 MB, 8192 — the ceiling — is 536 MB: the largest thing one assignment can do to a process, which is why the setter says so above the default. 0 until shadows are first turned on; after that it follows `shadow.size` up and down, but `three.light.shadow.enabled = false` keeps the last map rather than freeing it.",
		"materials": "Materials built and not yet collected; the two built-in ones are not counted. A material holds a compiled pipeline until `material.dispose()`, so a script that builds one per run and drops the handle grows this forever — the host says so past 64. It falls when the material is collected, which is after both the dispose and the last mesh naming it, so disposing while something still draws with it leaves the number where it was.",
		"culledLastFrame": "Instances the camera frustum dropped in the last `render()`. Meaningful with shadows on: the shadow pass has its own draw list against the light's box.",
		"shadowCulled": "Instances neither pass drew — outside the camera frustum and outside the light's box. 0 with shadows off, and smaller than `culledLastFrame`, since a caster the camera cannot see is still drawn into the map.",
		"shadowDraws": "Draw calls the last shadow pass made, 0 with shadows off. Roughly `drawCalls` minus the transparent buckets and helpers, so it is what shadows cost in draws. With static casters it counts the movers alone.",
		"shadowStaticDraws": "Draw calls into the cached half of the shadow map, and 0 on every frame that did not rebuild it — which should be nearly all of them. Equal to the caster count every frame means something is invalidating the cache: an unsettled camera, or a static node still being moved.",
		"skinnedDraws": "Draw calls whose geometry is posed by a skeleton.",
		"skinnedInstances": "Characters in those draws. A hundred here with `skinnedDraws` at 1 is the crowd working as intended.",
		"preskinnedInstances": "Of those, the ones routed through the compute pass — `instantiate({ skinning: 'compute' })`. The expensive kind: each holds a posed copy of its mesh per frame in flight and is a draw call of its own.",
		"poseBytes": "Device memory holding baked animation poses, uploaded once per rigged file and shared by every copy. There is no per-frame palette upload behind a baked character, which is why a hundred of them is affordable.",
		"gpuMs": "Milliseconds the GPU spent on the frame you just asked for, on the GPU's own clock. `three.render()` and a screenshot each leave their own measurement, so render first and read after. 0 before anything is drawn and 0 for a run with no device — use `renderSize()` to tell those apart. The span is the whole submission, blit or readback included.",
		"prepareMs": "Of `gpuMs`: uploads, the frame's buffer writes and compute skinning. Everything before the first pass.",
		"shadowMs": "Of `gpuMs`: the shadow map, 0 with shadows off. Worth looking at first outdoors — the map is fitted around the whole scene, so a wide level pays for texels nowhere near the camera and `three.light.shadow.size` is the knob.",
		"sceneMs": "Of `gpuMs`: the pass that draws the picture.",
		"postMs": "Of `gpuMs`: the post chain, 0 with no post shader.",
		"presentMs": "Of `gpuMs`: getting the finished image out — the blit to the window, or the readback behind a screenshot. The five add up to `gpuMs`, so anything unaccounted for is a bug."
	},
	"intersection": {
		"object": "The Mesh that was hit. Null only for a node this script did not build — one opened from the command line.",
		"name": "Its name, which identifies it even when `object` is null.",
		"distance": "World units along the ray. Comparable across objects however each of them is scaled.",
		"point": "Where the ray met the surface, world space, as a Vector3.",
		"normal": "The surface normal there, world space, unit length."
	},
	"example": "const scene = new three.Scene();\n\n// One geometry per mesh is fine — the same numbers are the same asset,\n// so this whole grid is a single instanced draw call.\nfor (let x = -4; x <= 4; x++) {\n  for (let z = -4; z <= 4; z++) {\n    const cube = new three.Mesh(new three.BoxGeometry(1, 1, 1));\n    cube.position.set(x * 1.5, 0, z * 1.5);\n    cube.scale.y = 1 + Math.abs(x + z) * 0.4;   // scale is free, a new size is not\n    scene.add(cube);\n  }\n}\n\nconst ball = new three.Mesh(\n  new three.SphereGeometry(1.2, 48, 24),\n  new three.ShaderMaterial({\n    uniforms: { tint: [1, 0.4, 0.2] },\n    fragment: \"float3 shade(Surface s) { return lambert(s.normal) * tint; }\",\n  }),\n);\nball.position.y = 3;\nscene.add(ball);\n\nthree.camera.frameAll();\nthree.render(scene, three.camera);\nthree.debug.write(scene.stats());   // { drawCalls: 2, uniqueMeshes: 2, instances: 82, ... }",
	"exampleFromFile": "const kit = three.load(\"assets/kit.glb\");\nconst wall = kit.mesh(\"wall_corner_02\");\nconst scene = new three.Scene();\nfor (let i = 0; i < 12; i++) {\n  const m = new three.Mesh(wall);\n  m.position.set(i * 2, 0, 0);\n  m.rotation.y = Math.PI / 2;\n  scene.add(m);\n}\nthree.camera.frameAll();\nthree.render(scene, three.camera);\nthree.debug.write(scene.stats());   // { drawCalls: 1, instances: 12, ... }"
};

export const SECTIONS = [
	{
		"key": "differences",
		"blurb": "Where this is not Three.js. Read this section first — nearly every script that fails does so against one of these."
	},
	{
		"key": "classes",
		"blurb": "The constructors, their properties and their methods."
	},
	{
		"key": "functions",
		"blurb": "Everything on `three` itself, keyed by its whole call."
	},
	{
		"key": "stats",
		"blurb": "What each number from `scene.stats()` counts."
	},
	{
		"key": "intersection",
		"blurb": "The record a raycast or a pick answers with."
	},
	{
		"key": "keys",
		"blurb": "The names `three.input` and `three.onKey` match against. This list is read out of the engine's own key table, so it is the list that actually matches."
	},
	{
		"key": "example",
		"blurb": "A complete scene, with the draw-call count it ends on."
	},
	{
		"key": "exampleFromFile",
		"blurb": "The same, from a glTF file."
	}
];
